# Actionable secret scan scope

The blocking security validation checks changed JavaScript, TypeScript, and PHP source files for obvious hardcoded secret assignments.

Legacy repository-wide matches remain advisory in self-validation. Dependency folders, fixtures, scratch files, documentation, and example files are excluded from the blocking changed-file scan.
