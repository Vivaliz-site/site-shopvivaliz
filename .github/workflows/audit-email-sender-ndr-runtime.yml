name: DEPRECATED - Audit email sender and NDR runtime

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This workflow is deprecated because it could remove sender_blocked.pause without live Exchange verification. Use the verified hourly sender recovery workflow instead."
          exit 1
