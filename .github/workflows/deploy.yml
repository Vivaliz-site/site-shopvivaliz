name: Deploy Automático Seguro - Shop Vivaliz

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: shopvivaliz-deploy-dev
  cancel-in-progress: false

jobs:
  web-deploy:
    name: Sincronizar com a HostGator Dev
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Baixar o código do repositório
        uses: actions/checkout@v4.2.2

      - name: Validar secrets obrigatórios
        shell: bash
        run: |
          missing=0

          check_secret() {
            local name="$1"
            local value="$2"
            if [ -z "$value" ]; then
              echo "::error::Secret obrigatório ausente: $name"
              missing=1
            else
              echo "Secret configurado: $name"
            fi
          }

          check_secret "FTP_SERVER" "${{ secrets.FTP_SERVER }}"
          check_secret "FTP_USERNAME" "${{ secrets.FTP_USERNAME }}"
          check_secret "FTP_PASSWORD" "${{ secrets.FTP_PASSWORD }}"
          check_secret "FTP_PORT" "${{ secrets.FTP_PORT }}"
          check_secret "FTP_REMOTE_DIR" "${{ secrets.FTP_REMOTE_DIR }}"
          check_secret "SQUAD_TOKEN" "${{ secrets.SQUAD_TOKEN }}"
          check_secret "ANTHROPIC_API_KEY" "${{ secrets.ANTHROPIC_API_KEY }}"
          check_secret "OPENAI_API_KEY" "${{ secrets.OPENAI_API_KEY }}"
          check_secret "GEMINI_API_KEY" "${{ secrets.GEMINI_API_KEY }}"

          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

      - name: Gerar .env temporário para o ambiente dev
        shell: bash
        run: |
          cat > .env <<'EOF'
          SQUAD_TOKEN=${{ secrets.SQUAD_TOKEN }}
          ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}
          EOF
          sed -i 's/^          //' .env
          chmod 600 .env
          echo "Arquivo .env temporário gerado para deploy. Conteúdo omitido."

      - name: Enviar via FTP Deploy Action
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          protocol: ftp
          local-dir: ./
          server-dir: ${{ secrets.FTP_REMOTE_DIR }}
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/vendor/**
            **/*.sql
            **/*.zip
            **/.env.*
            **/login_config.json
            **/*.duck
            **/*secret*
            **/*token*
            **/*password*
            **/*senha*

      - name: Aguardar publicação no servidor
        run: sleep 20

      - name: Teste pós-deploy básico
        shell: bash
        run: |
          BASE="https://dev.shopvivaliz.com.br"
          DEPLOY_FAILED=false

          echo "Testando home do ambiente dev..."
          if ! curl -fsS "$BASE/" >/dev/null; then
            echo "❌ Falha ao acessar a home do ambiente dev."
            DEPLOY_FAILED=true
          else
            echo "✅ Home dev acessível."
          fi

          echo "Testando arquivo de validação do CNPJ, se existir..."
          if curl -fsS "$BASE/cnpj-rodape-teste.html" | grep -q "49.903.300/0001-70"; then
            echo "✅ CNPJ validado no arquivo de teste."
          else
            echo "⚠️ Arquivo de CNPJ não encontrado ou conteúdo diferente. Não bloqueando deploy."
          fi

          echo "Testando agente Olist, se existir..."
          if curl -fsS "$BASE/api/agent/olist-zero-id-repair.php" | grep -q '"agent"'; then
            echo "✅ Agente Olist respondeu."
          else
            echo "⚠️ Agente Olist não disponível. Não bloqueando deploy."
          fi

          echo "Testando agente de manual do admin, se existir..."
          if curl -fsS "$BASE/api/agent/admin-manual-audit.php" | grep -q 'admin_manual_audit'; then
            echo "✅ Agente de manual do admin respondeu."
          else
            echo "⚠️ Agente de manual do admin não disponível. Não bloqueando deploy."
          fi

          if [ "$DEPLOY_FAILED" = true ]; then
            echo "❌ Deploy publicado, mas teste básico falhou."
            exit 1
          fi

          echo "✅ Deploy concluído com sucesso."

      - name: Diagnóstico de IA em caso de falha
        if: failure()
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        shell: bash
        run: |
          echo "🚨 Workflow falhou. Iniciando diagnóstico se o arquivo existir."

          if [ -f "ai_collaboration.py" ]; then
            python -m pip install --upgrade pip
            pip install google-genai openai anthropic
            python ai_collaboration.py
          else
            echo "⚠️ ai_collaboration.py não encontrado. Diagnóstico de IA ignorado."
          fi
