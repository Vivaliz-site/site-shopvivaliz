name: DEPRECATED - MEI email DB restart V025 worker v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Deprecated: this workflow could clear sender_blocked.pause directly after a DB restart. Use MEI Email Safe Hourly Repair Deploy."
          exit 1
