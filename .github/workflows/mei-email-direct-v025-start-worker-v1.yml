name: DEPRECATED - MEI email direct V025 start worker v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Deprecated: this workflow could clear sender_blocked.pause directly after DB maintenance. Use MEI Email Safe Hourly Repair Deploy."
          exit 1
