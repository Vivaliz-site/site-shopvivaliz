name: DEPRECATED - MEI email final recovery start v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Deprecated: this recovery could delete sender_blocked.pause without the current verified Exchange proof. Use MEI Email Safe Hourly Repair Deploy."
          exit 1
