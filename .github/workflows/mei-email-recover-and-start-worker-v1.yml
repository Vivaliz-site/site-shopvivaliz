name: DEPRECATED - MEI email recover and start worker v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This workflow is deprecated because it could remove the sender-block sentinel after database maintenance without current Exchange proof. Use the verified hourly recovery path instead."
          exit 1
