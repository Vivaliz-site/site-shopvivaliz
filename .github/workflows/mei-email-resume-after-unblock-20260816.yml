name: DEPRECATED - MEI email resume after unblock 20260816

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Deprecated: direct deletion of sender_blocked.pause is forbidden. Use the verified hourly sender recovery path."
          exit 1
