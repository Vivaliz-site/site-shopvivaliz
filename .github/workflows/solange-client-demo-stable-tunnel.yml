name: Solange client demo stable tunnel (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled state
        run: |
          echo 'Stable tunnel automation is intentionally disabled.'
          echo 'CLOUDFLARE_API_TOKEN returned HTTP 401 on 2026-08-31.'
          echo 'No DNS record or named tunnel was created.'
          echo 'Current client demo continues through the existing Quick Tunnel.'
