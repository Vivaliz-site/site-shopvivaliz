name: DEPRECATED - Resume email worker after verified restriction clear 20260818

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe recovery path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This workflow is deprecated because it could delete sender_blocked.pause directly. Use the verified hourly sender recovery flow, which requires live Exchange proof before clearing a stale sentinel."
          exit 1
