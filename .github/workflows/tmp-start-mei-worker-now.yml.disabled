name: DEPRECATED - Start MEI worker now with quota guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete unsafe start path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Deprecated: this workflow did not account for the external quota ledger and did not enforce the verified sender-block recovery path. Use MEI Email Safe Hourly Repair Deploy."
          exit 1
