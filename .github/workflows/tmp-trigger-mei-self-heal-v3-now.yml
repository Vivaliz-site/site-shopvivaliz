name: Trigger MEI Self-Heal V3 Now

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tmp-trigger-mei-self-heal-v3-now.yml'

permissions:
  actions: write
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch canonical fail-closed self-heal
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run mei-email-hourly-self-heal-v3.yml --repo "${GITHUB_REPOSITORY}" --ref main
