# Never persist URL query strings in access logs. Secrets for providers that
# only support URL-based webhook authentication must remain out of log files.
LogFormat "%h %l %u %t \"%m %U %H\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\"" combined
