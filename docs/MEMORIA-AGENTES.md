# Memória compartilhada dos agentes

> **Todo agente (Claude, GPT, Gemini, ou qualquer outro) que trabalhar neste repo deve
> ler este arquivo ANTES de começar, e adicionar uma entrada aqui ao final da sessão se
> tiver aprendido algo não-óbvio.**
>
> Isso existe porque múltiplos agentes autônomos diferentes trabalham neste repo em
> sessões isoladas, sem memória compartilhada entre si. Sem um lugar único combinado,
> o mesmo bug/API/comportamento contra-intuitivo é redescoberto do zero repetidas
> vezes. O objetivo deste arquivo é quebrar esse ciclo: cada sessão deve sair daqui
> mais capaz do que entrou, e a próxima sessão (de qualquer agente) deve começar já
> sabendo o que a anterior descobriu.

## Como usar

- **Antes de investigar um bug ou integrar com um sistema externo**, procure aqui por
  uma entrada relacionada (Ctrl+F pelo nome do sistema/arquivo/sintoma).
- **Ao final de uma sessão**, se você descobriu algo que não era óbvio pelo código
  (um campo de API com nome diferente do esperado, um enum invertido, uma race
  condition, um comportamento assíncrono, uma decisão de arquitetura e o porquê),
  adicione uma entrada nova no topo da lista abaixo, seguindo o formato:

```
### AAAA-MM-DD — Título curto do que foi aprendido
**Sistema/arquivo:** onde isso se aplica
**O que descobri:** o fato em si, direto ao ponto
**Por quê importa:** o que dá errado se você não souber disso
**Ver também:** link pra doc mais funda, se existir (ex: docs/TINY-ERP-API-V3.md)
```

- Entradas específicas e extensas (ex: schema completo de uma API) vão em um arquivo
  dedicado dentro de `docs/` (ex: `docs/TINY-ERP-API-V3.md`) — aqui só o resumo com
  link. Não duplique o conteúdo inteiro aqui, isso deixaria o arquivo ilegível.
- Não remova entradas antigas a não ser que estejam **confirmadas** obsoletas (ex: o
  bug foi corrigido de vez e não pode mais acontecer). Se um problema foi corrigido
  mas pode voltar a acontecer (ex: alguém reverter o fix sem saber), mantenha a
  entrada e marque `[RESOLVIDO em <data>, ver commit/PR]`.

---

### 2026-08-14 — git-auto-sync local acumulou 62 commits sem push (branch protection bloqueia push direto)
**Sistema/arquivo:** repo local em C:\SITE-SHOPVIVALIZ, workflow de auto-sync (ver `windows-auto-push-setup.md` na memória do usuário)
**O que descobri:** o `main` local estava 61-62 commits à frente de `origin/main` e o daemon de auto-sync não estava conseguindo enviar nada — `git push origin main` falha com "pre-push: push direto para branch protegida bloqueado: main". O daemon aparentemente vinha commitando local silenciosamente sem notar a falha do push. Todo esse trabalho não estava perdido, só não publicado.
**Por quê importa:** qualquer agente que rode `git push origin main` direto vai falhar; o caminho é sempre branch + PR + merge (squash), como o CLAUDE.md já manda. Se alguém tentar "resolver" a divergência com `git reset --hard origin/main` **isso descarta os commits locais não enviados** (recuperável via `git reflog`, mas evite — prefira `git merge origin/main` para preservar o histórico local). O backlog de 62 commits ainda existe e não foi resolvido nesta sessão (fora de escopo); só extraí e enviei via PR o fix isolado de que eu precisava (`git diff <base> <commit> -- <arquivo> | git apply`).
**Ver também:** —

### 2026-08-13 — Prompt de Otimização de Cadastro não ensinava a IA a regra que o gate valida
**Sistema/arquivo:** `admin/catalog-optimization/api/optimize_catalog.php` (`ai_catalog_quality_report` / `ai_catalog_title_starts_with_identity` / `ai_catalog_build_user_prompt`)
**O que descobri:** o gate de qualidade exige que `optimized_title` comece por um "candidato de identidade" (marca, marca+modelo, ou as 3 primeiras palavras do nome original, todos calculados por `ai_catalog_identity_candidates()` com normalização de acento/caixa). O prompt enviado à IA nunca mostrava esses candidatos — só a instrução genérica "Produto + Marca + Modelo" do texto de política do canal — então a IA não tinha como acertar o prefixo exato. Em produção isso derrubava 60 dos 68 rascunhos `failed`/`rejected` (todos por `title_starts_with_identity`). Corrigido em `main` via PR #956: o prompt agora recebe a lista literal de candidatos aceitos. Testado ao vivo: mesmo produto/provedor, quality_score foi de reprovado para 100.
**Por quê importa:** um "hard quality gate" que valida algo que o gerador nunca foi instruído a fazer é um contrato quebrado silencioso — parece um problema de IA/prompt ruim, mas é um bug de integração entre o validador e o prompt-builder. Ao adicionar qualquer novo `check` em `ai_catalog_quality_report`, sempre espelhar a regra explicitamente no prompt (`ai_catalog_build_user_prompt`), nunca assumir que uma instrução vaga na política do canal é suficiente.
**Ver também:** `admin/catalog-optimization/repair_hard_quality_pending.php` reprocessa os itens presos por esse gate sem publicar nada.


### 2026-08-13 — Título do anúncio do ML não é editável quando o item tem `family_name`
**Sistema/arquivo:** API de Items do Mercado Livre; `scripts/ml-seo-optimizer.py`, `scripts/ml-image-optimizer.py`
**O que descobri:** todos os 158 anúncios ativos sem venda do seller são `user_product_listing` (têm `family_name` + `user_product_id`). Nesses itens `PUT /items/{id}` com `title` responde 400 `"You cannot modify the title if the item has a family_name"`, e mandar `family_name` também é recusado (400 `BODY_INVALID_FIELDS`) **mesmo com o valor idêntico ao atual**. `PUT /user-products/{id}` não existe (404). Em compensação, o ML **recompõe o título a partir dos atributos**: preencher um atributo com tag `allow_variations` faz o valor ser anexado ao título (comprovado: `FINISH=Polido` virou " ... Prateado Polido" no título). O que aceita PUT é `attributes`, `pictures` e `/items/{id}/description`.
**Por quê importa:** qualquer rotina de SEO que planeje "reescrever o título via API" vai bater em 400 no primeiro item e parecer bug de autenticação/payload. O caminho real é ficha técnica + descrição — e mexer em atributo de variação tem efeito colateral no título, então tem que reler o título depois do PUT e reverter se piorar.
**Ver também:** `docs/MERCADO-LIVRE-API.md` (limites por categoria, filtros de atributo, health 404, CDN de imagem bloqueado para download externo)


## Entradas

### 2026-08-19 — Ciclo 6h de otimização Shopee: reconfirmação após 2 dias sem checagem, nenhuma mudança de fundo (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança funcional)
**O que descobri:** checagem completa do ciclo agendado de hoje (gap de 2 dias desde a reconfirmação de 08-17). Estado idêntico: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` (por `sort`) continua `shopee-listings-20260726-080756.json`; (4) `mcp__github__actions_list` (`shopee-production-seo.yml`) retorna as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165), todas `conclusion: failure` — nenhuma execução nova em quase 3 semanas; (5) `origin/main` está em `04dcaf5`, sem nenhum commit tocando os arquivos de produção Shopee desde 08-17. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum. Achado novo desta sessão: o checkout local tinha uma branch `main` local órfã e muito desatualizada (em `291472f`, 277/53 commits divergente de `origin/main`) sobrando de alguma sessão anterior — não toquei nela (nem reset nem push), só trabalhei a partir de uma branch nova criada direto de `origin/main` para não arriscar sobrescrever histórico.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com sucesso real) não disparou — reportar seria spam. Bloqueio segue idêntico desde 2026-07-26 (mais de 3 semanas). A tarefa dos 12 passos (CTR, A/B testing, trending keywords, reordenar imagens etc.) continua sem dado real de vendas/CTR disponível neste ambiente — qualquer "otimização" feita sem esses dados seria fabricação, então nenhuma foi feita. Separadamente: se alguém for limpar branches locais órfãs num checkout desta VM/sandbox, a `main` local em `291472f` é uma delas — não é trabalho em progresso de ninguém, é só uma referência velha do clone.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9, entrada de 2026-08-17 abaixo e anteriores

### 2026-08-17 — Ciclo 6h de otimização Shopee: reconfirmação após 4 dias sem checagem, nenhuma mudança de fundo (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança funcional)
**O que descobri:** checagem completa do ciclo agendado de hoje (gap de 4 dias desde a última reconfirmação em 08-13). O sha `4c805f1` referenciado na entrada anterior não existe mais em `origin/main` — mais uma reescrita de histórico (força-push), consistente com o padrão já registrado em entradas de 07-30/07-31/08-06. Estado idêntico apesar disso: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/` (só `shopee-optimizer-safety.yml`, `shopee-production-seo.yml`, `shopee-runtime-health.yml`); (3) artefato mais recente em `listings/` (por `sort`) continua `shopee-listings-20260726-080756.json`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"`, `total_products: 0`; (4) `mcp__github__actions_list` (`shopee-production-seo.yml`) retorna as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165), todas `conclusion: failure` — nenhuma execução nova em mais de 2 semanas. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com sucesso real) não disparou — reportar seria spam. Bloqueio segue idêntico desde 2026-07-26 (mais de 3 semanas). A tarefa dos 12 passos (CTR, A/B testing, trending keywords, reordenar imagens etc.) continua sem dado real de vendas/CTR disponível neste ambiente — qualquer "otimização" feita sem esses dados seria fabricação, então nenhuma foi feita.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9, entrada de 2026-08-13 abaixo e anteriores

### 2026-08-13 — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde a reconfirmação de 08-12 19:11 UTC (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança funcional)
**O que descobri:** checagem do ciclo agendado de hoje. `git log 4c805f1..HEAD` traz só 4 commits, todos sobre instalação do Fred-Win MCP startup (`aecffab`, `4b0f4d9`, `f5099a2`, `bb4ff78`) — nenhum toca Tiny/Shopee/Olist. Confirmei mesmo assim os 4 pontos de sempre: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` (por `sort`) continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"`, `total_products: 0`; (4) `mcp__github__actions_list` (`shopee-production-seo.yml`) retorna `total_count: 5`, as mesmas execuções de 2026-07-30 (IDs 30570700034–30585266165), todas `conclusion: failure` — nenhuma execução nova há mais de duas semanas. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com sucesso real) não disparou — reportar seria spam. Estado idêntico há quase 3 semanas (desde 07-26). Próximos ciclos: seguir o atalho já registrado na entrada de 08-12 19:11 UTC (só `git log <último-sha>..HEAD` vazio já basta pra decidir não notificar, sem reabrir a checagem completa).
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9, entrada de 2026-08-12 (19:11 UTC) abaixo e anteriores

### 2026-08-12 (19:11 UTC) — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde a reconfirmação de 13:17 UTC de hoje (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança funcional)
**O que descobri:** checagem rápida do ciclo agendado das 19:00 UTC de hoje. `git log 0ddd42e..HEAD` vazio — nenhum commit desde a última reconfirmação (13:17 UTC), então nada mudou por definição: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json` (`status: partial`, `total_products: 0`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"`); (4) `mcp__github__actions_list` (`shopee-production-seo.yml`) retorna as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165), todas `conclusion: failure` — nenhuma execução nova em quase duas semanas. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com sucesso) não disparou — reportar seria spam. Dado que esta é a 3ª reconfirmação idêntica só hoje e o estado é o mesmo desde 07-26 (mais de 2 semanas), próximos ciclos podem pular a checagem completa e só confirmar `git log <último-sha>..HEAD` vazio antes de decidir não notificar — só vale reabrir a checagem completa se algo mudar.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9, entrada de 2026-08-12 (13:17 UTC) acima e anteriores abaixo

### 2026-08-12 (13:17 UTC) — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde a reconfirmação de 07:14 UTC de hoje (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança funcional)
**O que descobri:** checagem rápida do ciclo agendado das 13:00 UTC de hoje. `git log 58621c0..HEAD` vazio — nenhum commit desde a última reconfirmação (07:14 UTC), então nada mudou por definição: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json` (`status: partial`, `total_products: 0`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"`); (4) `mcp__github__actions_list` (`shopee-production-seo.yml`) retorna as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165), todas `conclusion: failure` — nenhuma execução nova. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com sucesso) não disparou — reportar seria spam.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9, entrada de 2026-08-12 (07:14 UTC) acima e anteriores abaixo

### 2026-08-12 — Ciclo 6h de otimização Shopee: reconfirmação após 5 dias sem checagem, nenhuma mudança de fundo (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25 a 08-07 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança funcional)
**O que descobri:** checagem completa do ciclo agendado de hoje (gap de 5 dias desde a última reconfirmação em 08-07 19:00 UTC). Estado idêntico: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"`, `total_products: 0`; (4) `mcp__github__actions_list` (`list_workflow_runs`, `shopee-production-seo.yml`) retorna `total_count: 5`, exatamente as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165) já documentadas, todas `conclusion: failure` — nenhuma execução nova em quase duas semanas; (5) único commit tocando caminhos Shopee desde 08-07 é `e43be33` ("restore trailing newline in production workflow", 08-11), mas o diff mostra `shopee-production-seo.yml` como arquivo inteiramente novo — sintoma de reescrita/squash de histórico (mesmo fenômeno já registrado em 07-30/31), não mudança de conteúdo; comparei o corpo do workflow e é byte-a-byte igual ao já auditado. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com "Apply SEO to real Shopee catalog" != skipped/success) não disparou — reportar seria spam. Próximas sessões: mesma checagem rápida de sempre; se o gap entre reconfirmações continuar crescendo, não é motivo por si só para notificar.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9, entrada de 2026-08-07 (19:00 UTC) e anteriores abaixo

### 2026-08-07 (19:00 UTC) — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde a reconfirmação de 13:00 UTC de hoje (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25 a 08-07 (13:00 UTC) abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança)
**O que descobri:** checagem rápida do ciclo agendado das 19:00 UTC de hoje. Estado idêntico ao da reconfirmação anterior (commit `c0e1094`, 08-07 13:00 UTC): (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, sem novidade; (4) `mcp__github__actions_list` (`list_workflow_runs`, `shopee-production-seo.yml`, id 323963581) retorna `total_count: 5`, exatamente as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165) já documentadas — nenhuma execução nova, todas `conclusion: failure`; (5) `git log c0e1094..HEAD` (29 commits, sobre Fred-Win remote access/tunnel e Google Ads) não toca `scripts/shopee_*`, `scripts/utils/shopee_client.py`, `.github/workflows/*shopee*` nem `docs/TINY-TOKEN-RENEWAL-SETUP.md` — nenhum commit relacionado a Tiny/Shopee/Olist no período. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com apply != skipped) não disparou — reportar seria spam. Próximas sessões: mesma checagem rápida de sempre.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9 (histórico completo de ciclos), entrada de 2026-08-07 (13:00 UTC) e anteriores abaixo

### 2026-08-07 (13:00 UTC) — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde a reconfirmação de 07:14 UTC de hoje (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25 a 08-07 (07:14 UTC) abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança)
**O que descobri:** checagem rápida do ciclo agendado das 13:00 UTC de hoje. Estado idêntico ao da reconfirmação anterior (commit `cdba106`, 08-07 07:14 UTC): (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, sem novidade; (4) `mcp__github__actions_list` (`list_workflow_runs`, `shopee-production-seo.yml`, id 323963581) retorna `total_count: 5`, exatamente as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165) já documentadas — nenhuma execução nova, todas `conclusion: failure`; (5) `git fetch origin main` não trouxe nenhum commit novo em relação a `HEAD` (`cdba106` já era a ponta de `origin/main`) — nenhum commit relacionado a Tiny/Shopee/Olist desde a última checagem. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com apply != skipped) não disparou — reportar seria spam. Próximas sessões: mesma checagem rápida de sempre.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9 (histórico completo de ciclos), entrada de 2026-08-07 (07:14 UTC) e anteriores abaixo

### 2026-08-07 — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde o ciclo das 19:00 UTC de 08-06 (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25 a 08-06 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança)
**O que descobri:** checagem rápida do ciclo agendado de hoje. Estado idêntico ao da última reconfirmação (commit `5cf05af`, 08-06 19:09 UTC): (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, sem novidade; (4) `mcp__github__actions_list` (`list_workflow_runs`, `shopee-production-seo.yml`, id 323963581) retorna `total_count: 5`, exatamente as mesmas 5 execuções de 2026-07-30 (IDs 30570700034–30585266165) já documentadas — nenhuma execução nova, todas `conclusion: failure`; (5) `git log 5cf05af..origin/main` (40 commits) não toca `scripts/shopee_*`, `scripts/utils/shopee_client.py`, `.github/workflows/*shopee*` nem `docs/TINY-TOKEN-RENEWAL-SETUP.md` — nenhum commit relacionado a Tiny/Shopee/Olist no período. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com apply != skipped) não disparou — reportar seria spam. Próximas sessões: mesma checagem rápida de sempre.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), `docs/HISTORICO-AGENTES-SHOPEE.md` seção 9 (histórico completo de 24 ciclos), entrada de 2026-08-06 (19:00 UTC) e anteriores abaixo

### 2026-08-06 (19:00 UTC) — Ciclo 6h de otimização Shopee: reconfirmação, nenhuma mudança desde o ciclo das 04:51 UTC de hoje (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25 a 08-06 (04:51 UTC) abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (sem mudança)
**O que descobri:** disparo do ciclo 6h de 19:00 UTC, poucas horas após a reconfirmação anterior de hoje (commit `da01979`, 04:51 UTC) — desta vez o hiato entre reconfirmações voltou ao padrão de ~6h. Checagem rápida confirmou estado idêntico: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/`; (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, sem novidade; (4) `mcp__github__actions_list` (`list_workflow_runs`, workflow `shopee-production-seo.yml`, id 323963581) retorna `total_count: 5`, exatamente as mesmas 5 execuções de 2026-07-30 (IDs 30570700034 a 30585266165) já documentadas — nenhuma execução nova, todas `conclusion: failure`; (5) `git fetch origin main` trouxe outro `forced update` (653fcc6→4606ea1), mas os commits novos em relação ao ciclo das 04:51 UTC (`5b96b50`, `4606ea1`) são sobre editor de rascunho de título no admin de catálogo, não relacionados a Tiny/Shopee. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** critério de notificação (workflows recriados, artefato novo com erro diferente, ou `shopee-production-seo.yml` com apply != skipped) não disparou de novo — reportar seria spam. Próximas sessões: mesma checagem rápida de sempre.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entrada de 2026-08-06 (04:51 UTC) e anteriores abaixo

### 2026-08-06 — Ciclo 6h de otimização Shopee: mesmo estado desde 07-31, nenhuma execução nova do `shopee-production-seo.yml` (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25 a 07-31 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml`/`shopee-optimizer-safety.yml` (novos, sem mudança)
**O que descobri:** disparo agendado de novo neste sandbox, 6 dias após a última reconfirmação (07-31) — hiato maior que o padrão de 6h, mas nada mudou no meio tempo. Confirmado: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes de `.github/workflows/` (confirmado tanto no checkout local quanto via `mcp__github__actions_list` contra o repo real — 99 workflows ativos, nenhum com esse nome); (3) artefato mais recente em `listings/` continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"`, `total_products: 0`; (4) as 5 execuções do `Shopee SEO Production Apply` (`shopee-production-seo.yml`, id 323963581) continuam sendo as mesmas 5 de 2026-07-30 já documentadas na entrada de 07-31 — nenhuma execução nova desde então, todas `conclusion: failure` com o passo real de apply pulado; (5) `git fetch origin main` trouxe um `forced update` (653fcc6→042994f) — histórico reescrito de novo, mas commits recentes (042994f e vizinhos) são sobre normalização de credenciais de IA (Anthropic/OpenAI/Gemini), não relacionados a Tiny/Shopee. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum.
**Por quê importa:** o bloqueio segue idêntico ao de 07-25/07-31 e o critério de notificação ((a) workflows `fetch/optimize-shopee-listings.yml` recriados, ou (b) artefato novo em `listings/` com erro diferente, ou (c) `shopee-production-seo.yml` produzir uma execução real com `Apply SEO to real Shopee catalog` != `skipped`) não disparou — reportar de novo seria spam. Próximas sessões: mesma checagem rápida das entradas anteriores; se o hiato entre execuções deste ciclo continuar crescendo (era a cada ~1 dia, virou 6 dias), pode valer mencionar ao usuário que a tarefa agendada "Agente de Otimização Inteligente Shopee" parece estar rodando com menos frequência que os 6h configurados — mas isso é sobre o agendador, não sobre o pipeline Shopee em si.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2, desatualizada — não menciona `shopee-production-seo.yml`), entradas de 2026-07-25 a 07-31 abaixo

### 2026-07-31 — Ciclo 6h de otimização Shopee: pipeline antigo continua ausente, mas surgiu um novo pipeline manual `shopee-production-seo.yml` (não notifiquei — já é trabalho do próprio usuário)
**Sistema/arquivo:** `.github/workflows/{fetch,optimize}-shopee-listings.yml` (ainda ausentes), `.github/workflows/shopee-production-seo.yml` + `shopee-optimizer-safety.yml` (novos), `scripts/shopee_production_seo_apply.py`, `scripts/shopee_full_catalog_optimizer.py`, `scripts/utils/shopee_client.py`
**O que descobri:** checagem rápida do ciclo 6h de 01:00 UTC: (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio neste sandbox; (2) `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` continuam ausentes — nenhum artefato novo em `listings/` desde `20260726-080756`/`20260726-060921`. Ou seja, o bloqueio original (credencial OAuth2 do Tiny revogada, sem workflow agendado) **não mudou**. Porém, diferente das reconfirmações anteriores, hoje existem dois workflows Shopee novos que não existiam em nenhuma checagem anterior: `Shopee Optimizer Safety` (CI, criado 2026-07-30T14:20 -03) e `Shopee SEO Production Apply` (`workflow_dispatch` manual com `environment: shopee-production` + frase de confirmação obrigatória `APPLY_ALL_SHOPEE_PRODUCTS`, criado 2026-07-30T15:29 -03). Este pipeline novo é bem mais restrito que o antigo: só título/descrição (nunca preço/estoque, que são tratados como invariantes e bloqueados explicitamente no código), com backup + leitura de verificação + rollback automático em `scripts/shopee_production_seo_apply.py`. Confirmei via `mcp__github__actions_list`/`actions_get` que as 5 execuções desse workflow até agora (todas de ontem, 2026-07-30, todas disparadas por `fredmourao-ai`, autor dos commits `Frederico mourao <fredmourao@gmail.com>`) terminaram em `failure`, mas o passo real "Apply SEO to real Shopee catalog" ficou `skipped` em todas — ou seja, **nenhuma mutação real chegou a ser aplicada no catálogo** nessas tentativas; há também um passo "Publish first-product evidence to issue 598" (`.github/triggers/shopee-first-product-validation.json`), então o próprio usuário está testando isso manualmente via issue #598. Não fabriquei CTR/conversão nem editei título/descrição/imagem/preço de produto algum nesta sessão — a tarefa dos 12 passos (CTR, A/B testing, trending keywords) continua sem dado real disponível neste ambiente.
**Por quê importa:** o critério de notificação das entradas anteriores ("(a) workflows recriados") tecnicamente disparou, mas não são os workflows antigos voltando — é um pipeline novo e mais seguro (title/description-only, com rollback) que o próprio dono do repo está construindo e testando manualmente (commits e workflow_dispatch dele mesmo, ligados à issue #598), então notificá-lo seria redundante. Não confundir esse pipeline (`shopee-production-seo.yml`, manual, gated por frase de confirmação) com o antigo cron de 6h (`fetch/optimize-shopee-listings.yml`, automático, ainda ausente) — são sistemas diferentes; a tarefa agendada "Agente de Otimização Inteligente Shopee" continua sem infraestrutura automática pra rodar. Próximas sessões: se `shopee-production-seo.yml` passar a ter uma execução com `Apply SEO to real Shopee catalog` != `skipped` e `conclusion: success`, ou se `fetch/optimize-shopee-listings.yml` voltarem a existir, aí sim vale reavaliar se virou algo que a rotina de 6h deveria alimentar/reportar.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2 — desatualizada, ainda não menciona `shopee-production-seo.yml`), entradas de 2026-07-25 a 07-30 abaixo, `docs/audits/history-cleanup-validation.md` (contexto: reescrita de histórico por revogação de credencial em 2026-07-30/31, não relacionada ao Shopee)

### 2026-07-30 — Ciclo 6h de otimização Shopee: ainda parado, sexta reconfirmação seguida (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25/07-26/07-27/07-29/07-30 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml`, credencial `TINY_*`
**O que descobri:** mais um disparo do ciclo 6h, mesma checagem rápida (não reinvestiguei do zero): (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio; (2) `.github/workflows/` continua sem `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml`; (3) artefato mais recente em `listings/` (por `sort`, não `ls -t`) continua `shopee-listings-20260726-080756.json`, mesmo erro `"Falha ao renovar token: Invalid client or Invalid client credentials"` e `total_products: 0` — nenhum artefato novo desde a última reconfirmação. Nenhum dado de CTR/conversão fabricado, nenhum título/descrição/imagem/preço de produto editado. Não enviei notificação — estado idêntico ao já reportado; critério de novo aviso continua sendo (a) workflows recriados ou (b) artefato novo com erro diferente de "Invalid client or Invalid client credentials".
**Por quê importa:** evita a rotina de 6h gastar um ciclo inteiro reinvestigando um bloqueio já documentado e que só o usuário pode resolver (regenerar OAuth2 em `accounts.tiny.com.br`).
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entradas de 2026-07-25/07-26/07-27/07-29/07-30 abaixo

### 2026-07-30 — Ciclo 6h de otimização Shopee: ainda parado, quinta reconfirmação seguida (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25/07-26/07-27/07-29/07-30 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml`, credencial `TINY_*`
**O que descobri:** novo disparo do ciclo 6h, mesmo sandbox, mesma checagem rápida (não reinvestiguei do zero): (1) `env | grep -iE "TINY_|OLIST_|SHOPEE_|ANTHROPIC_API_KEY"` continua vazio; (2) `.github/workflows/` continua sem `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml`; (3) artefato mais recente em `listings/` (ordenado por nome) continua `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json` — nenhum novo desde a última reconfirmação. Nenhum dado de CTR/conversão fabricado, nenhum título/descrição/imagem/preço de produto editado. Não enviei notificação — estado idêntico ao já reportado ao usuário; critério de novo aviso continua sendo (a) workflows recriados ou (b) artefato novo com erro diferente de "Invalid client or Invalid client credentials".
**Por quê importa:** evita a rotina de 6h gastar um ciclo inteiro reinvestigando um bloqueio já documentado e que só o usuário pode resolver (regenerar OAuth2 em `accounts.tiny.com.br`).
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entradas de 2026-07-25/07-26/07-27/07-29/07-30 abaixo

### 2026-07-30 — Ciclo 6h de otimização Shopee: ainda parado, quarta reconfirmação seguida (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25/07-26/07-27/07-29 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml`, credencial `TINY_*`
**O que descobri:** disparo agendado de novo neste sandbox, sem `TINY_*`/`SHOPEE_*`/`ANTHROPIC_API_KEY` reais no `env` (confirmado via `env | grep -iE "TINY|SHOPEE|ANTHROPIC"` — nenhuma dessas variáveis presente). Repeti a checagem rápida recomendada pela entrada de 07-29 em vez de reinvestigar do zero: (1) `.github/workflows/` continua sem `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml`; (2) artefato mais recente em `listings/` (ordenando por nome, não por mtime — mtime não é confiável após `git clone` fresco) continua sendo `shopee-listings-20260726-080756.json`/`optimization-report-20260726-060921.json`, ambos com `"Falha ao renovar token: Invalid client or Invalid client credentials"` e `total_products: 0`; (3) `KNOWN_ISSUES.md` já cobre este estado exato. Não fabriquei CTR/conversão nem editei título/preço/listagem/imagens de produto algum — a tarefa agendada pede decisão autônoma baseada em dados reais de performance Shopee, que não existem neste ambiente. Não enviei notificação nova: nada mudou desde a reconfirmação de 07-29 (mesmo erro, mesmos workflows ausentes).
**Por quê importa:** próximas sessões deste ciclo de 6h: continuem só com a checagem rápida (`ls .github/workflows/ | grep shopee`, `ls listings/shopee-listings-*.json | sort | tail -1` — **usar `sort`, não `ls -t`**, já que mtime pós-clone não reflete a data real do arquivo). Notificar o usuário de novo só quando: (a) os workflows forem recriados, ou (b) surgir artefato novo em `listings/` com erro diferente de "Invalid client or Invalid client credentials".
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entradas de 2026-07-25/07-26/07-27/07-29 abaixo

### 2026-07-29 — Ciclo 6h de otimização Shopee: mesmo estado parado desde 2026-07-26, sem novidade (não notifiquei)
**Sistema/arquivo:** mesmo problema das entradas de 07-25/07-26/07-27 abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml`, credencial `TINY_*`
**O que descobri:** disparo agendado de novo neste sandbox, sem `TINY_*`/`SHOPEE_*`/`ANTHROPIC_API_KEY` reais no `env`. Antes de assumir que nada mudou, conferi: (1) `.github/workflows/` ainda não tem `fetch-shopee-listings.yml` nem `optimize-shopee-listings.yml` — seguem ausentes desde a consolidação de 07-26; (2) artefato mais recente em `listings/` continua sendo `shopee-listings-20260726-080756.json`, nenhum novo desde então (esperado, já que não há workflow rodando); (3) `KNOWN_ISSUES.md` já tem a seção "🔴 CRÍTICO: Pipeline de otimização/sincronização Shopee..." cobrindo exatamente este estado. Não fabriquei CTR/conversão nem editei título/preço/listagem de produto. Não enviei notificação nova — o problema é idêntico ao já reportado ao usuário em 2026-07-25 e reconfirmado em 07-26/07-27, e a orientação registrada nessas entradas é notificar de novo só se o erro mudar de tipo ou os workflows forem recriados.
**Por quê importa:** próximas sessões deste ciclo de 6h podem repetir só a checagem rápida (`ls .github/workflows/ | grep shopee`, `ls -t listings/shopee-listings-*.json | head -1`) em vez de re-investigar do zero. Só vale notificar o usuário de novo quando: (a) os workflows `fetch-shopee-listings.yml`/`optimize-shopee-listings.yml` forem recriados, ou (b) surgir um artefato novo em `listings/` com erro diferente de "Invalid client or Invalid client credentials" (sinal de que a credencial Tiny foi regenerada).
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entradas de 2026-07-25/07-26 abaixo

### 2026-07-26 — MERCADOPAGO_WEBHOOK_SECRET regrediu de novo (5ª vez) — pedido real aprovado ficou preso, sem impacto em outros clientes
**Sistema/arquivo:** `api/webhook-mercadopago.php`, `MERCADOPAGO_WEBHOOK_SECRET` (GitHub Actions secret, escopo de ambiente "Production")
**O que descobri:** teste E2E de compra real (vedante, `SV20260726155645987`, pago via Pix) ficou preso em `aguardando_pagamento` apesar do Mercado Pago ter enviado o webhook de pagamento 5 vezes (13:03–13:56) — todas rejeitadas com `401 invalid signature` (ver `shopvivaliz_access.log`/`shopvivaliz_error.log`). Mesmo padrão já documentado abaixo (2026-07-18/19), root cause idêntica (secret de ambiente Production desatualizado). Varri `shopvivaliz_error.log*` desde 19/07 (219 rejeições, 6 payment_ids reais distintos) e confirmei via `GET /v1/payments/{id}` (Access Token, não Webhook Secret) que **só este pedido de teste estava de fato aprovado e preso** — os outros 5 estavam `rejected`/`cancelled` no próprio Mercado Pago, ou seja, nenhum cliente real ficou com pedido pago sem confirmação. Recuperei o pedido preso SEM tocar na validação de assinatura: consultei `GET /v1/payments/{id}` pra confirmar `status=approved` + `external_reference` batendo, depois repliquei manualmente o que o webhook teria feito (atualizar JSON do pedido, espelhar MySQL `orders`, chamar `svtop_push_order_tiny()`, disparar `api/webhook-post-processor.php` pro e-mail) — script de recuperação rodado uma única vez via SSH, não commitado no repo.
**Por quê importa:** se esse padrão regredir de novo (6ª vez), não perca tempo re-diagnosticando — vá direto em `gh secret list --repo Vivaliz-site/site-shopvivaliz --env Production` conferir a data de atualização de `MERCADOPAGO_WEBHOOK_SECRET`, e ao corrigir use `gh secret set MERCADOPAGO_WEBHOOK_SECRET --env Production` (não o escopo de repositório, que não é lido pelo workflow `sync-oracle-vm-secrets.yml`). Pra recuperar pedidos presos sem esperar retry do MP, o caminho seguro é sempre `GET /v1/payments/{id}` com o Access Token (nunca desativar a validação de assinatura do endpoint).
**Ver também:** entrada de 2026-07-18/19 abaixo (histórico completo do bug), `docs/VM-SSH-ACCESS.md`

### 2026-07-26 — SMTP do servidor com falha de autenticação — nenhum e-mail transacional está sendo enviado
**Sistema/arquivo:** `OrderNotificationService.class.php`, `api/emails/send-order-notification.php`, credenciais `EMAIL_*`/`MAIL_*` (`.env` do servidor)
**O que descobri:** ao tentar reenviar o e-mail de aprovação de pagamento do pedido de teste recuperado (ver entrada acima), tanto o `OrderNotificationService` quanto o fallback SMTP direto falharam com `SMTP Error: Could not authenticate`. Não investiguei a causa exata (senha de app expirada é a suspeita mais provável) nem mexi na credencial — fica para o dono do negócio decidir/corrigir, já que é credencial de e-mail. **Não confirmado se isso afeta só este teste ou todos os e-mails transacionais do site** — precisa de verificação adicional (tentar outro evento/pedido) antes de generalizar.
**Por quê importa:** qualquer sessão futura que for verificar "o cliente recebeu o e-mail?" deve primeiro checar se esse problema ainda existe (`grep "Could not authenticate" /var/log/apache2/shopvivaliz_error.log` na VM) antes de assumir que o código de envio está com bug — o código pode estar 100% correto e a falha ser só a credencial SMTP.
**Ver também:** —

### 2026-07-26 — IDs de "Jadlog via Melhor envio" na doc estavam desatualizados/inativos
**Sistema/arquivo:** `docs/TINY-ERP-API-V3.md` (seção `GET /formas-envio`), `includes/tiny-order-push.php`
**O que descobri:** a conta Tiny tem múltiplos registros com o mesmo `nome` de transportadora (ex: "Jadlog via Melhor envio" com pelo menos 3 IDs diferentes), diferenciados por `situacao` (1=ativo/2=inativo). A tabela na doc listava um ID inativo (`337724757`); o código já usava o correto e ativo (`357119976`), confirmado ao vivo via `GET /formas-envio/357119976` (retorna `formasFrete[]` com os IDs de `.Package`/`.Com`) contra um pedido real (`SV20260726155645987`, Jadlog/.Package). Doc corrigida com a tabela completa e um aviso pra sempre conferir `situacao`+`gatewayLogistico` antes de usar um ID de `GET /formas-envio`.
**Por quê importa:** evita que o próximo agente "corrija" o código pra bater com a doc desatualizada (o que quebraria o payload de verdade). `formaEnvio` (transportadora) e `formaFrete` (serviço/modalidade) são campos distintos e ambos devem vir preenchidos por código numérico, nunca texto livre — já era assim no código, só a doc estava errada.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-26 — Workflow "Autonomous Safe Operations" falhava ao criar PR — bloqueio era política de ORGANIZAÇÃO, não do repositório
**Sistema/arquivo:** `.github/workflows/` (job `catalog-sync` do "Autonomous Safe Operations"), configuração de Actions da organização `Vivaliz-site`
**O que descobri:** o job falhava com `pull request create failed: GraphQL: GitHub Actions is not permitted to create or approve pull requests`. A configuração equivalente no repositório (`Settings → Actions → General → Workflow permissions`) **não é onde o bloqueio realmente está** quando a organização também tem essa política restrita — tentar mudar só no repo retorna `409 Conflict: The organization does not allow...`. Precisa mudar em `orgs/Vivaliz-site/actions/permissions/workflow` (`gh api --method PUT orgs/Vivaliz-site/actions/permissions/workflow -f default_workflow_permissions=read -F can_approve_pull_request_reviews=true`), que por sua vez exige um token `gh` com escopo `admin:org` (`gh auth refresh -h github.com -s admin:org`, fluxo interativo no navegador) E que a conta seja de fato owner/admin da organização — sem isso a API retorna 403 mesmo com o escopo certo. Corrigido em 2026-07-26 com autorização explícita do dono do negócio (mudança de política de organização inteira, categoria sensível de CI/CD/acesso); validado ao vivo disparando o workflow manualmente (`gh workflow run`) e confirmando o job `catalog-sync` completar com sucesso.
**Por quê importa:** se outro workflow no futuro falhar com o mesmo erro de "not permitted to create or approve pull requests", não perca tempo só na configuração do repo — confira a policy da organização primeiro (`gh api orgs/Vivaliz-site/actions/permissions/workflow`).
**Ver também:** —

### 2026-07-26 — Falha OAuth2 do Tiny ainda ativa (ciclo 6h de otimização Shopee); sem notificação nova (mesmo erro)
**Sistema/arquivo:** mesmo problema das entradas abaixo — `.github/workflows/{fetch,optimize}-shopee-listings.yml`, credencial `TINY_*`
**O que descobri:** disparo agendado do "Agente de Otimização Inteligente Shopee" (6h) rodou de novo neste sandbox sem `TINY_*`/`SHOPEE_*`/`ANTHROPIC_API_KEY` reais. Conferi o artefato mais recente do pipeline real antes de assumir que nada mudou: `listings/shopee-listings-20260725-185249.json` (gerado 18:52 UTC de ontem, o mais novo disponível) ainda mostra `"Falha ao renovar token: Invalid client or Invalid client credentials"` / `total_products: 0` — mesmo erro documentado em `KNOWN_ISSUES.md` desde 2026-07-03. Não fabriquei CTR/conversão nem editei listagem/preço de produto. Como o erro é idêntico ao já reportado e o usuário já foi notificado em 2026-07-25, **não enviei notificação nova** (seguindo a orientação explícita da entrada de 07-25: notificar de novo em todo ciclo de 6h com o mesmo erro seria spam).
**Por quê importa:** próximas sessões: só repita esta checagem rápida (`ls -t listings/shopee-listings-*.json | head -1` + olhar `errors`) e só notifique o usuário se o erro mudar de tipo (sinal de que a credencial Tiny foi regenerada) ou sumir (pipeline voltou a funcionar) — nesse caso, atualizar `KNOWN_ISSUES.md` também.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entradas de 2026-07-25 abaixo

### 2026-07-25 (13:xx UTC) — Falha OAuth2 do Tiny confirmada ainda ativa; entrada criada em `KNOWN_ISSUES.md`; usuário notificado
**Sistema/arquivo:** mesmo problema da entrada abaixo (2026-07-25 07:10 UTC) — `.github/workflows/{fetch,optimize}-shopee-listings.yml`, credencial `TINY_*`
**O que descobri:** disparo agendado do "Agente de Otimização Inteligente Shopee" (6h) rodou de novo neste sandbox sem `TINY_*`/`SHOPEE_*` — mesma limitação já documentada abaixo. Antes de assumir que nada mudou, conferi o artefato mais recente do pipeline real: `listings/shopee-listings-20260725-130116.json` (gerado 13:01 UTC, minutos antes desta sessão) ainda mostra `"Falha ao renovar token: Invalid client or Invalid client credentials"` e `total_products: 0`. Ou seja, o problema **não é fabricação de dados nem falta de sorte** — está confirmado em execução real e contínuo. Como a entrada de 07-10 apontou que faltava um registro em `KNOWN_ISSUES.md`, criei um lá (seção "🔴 CRÍTICO: Pipeline de otimização/sincronização Shopee"). Também enviei notificação push pro usuário, já que isso está parado há 3+ semanas sem ação e precisa de passo manual (regenerar client OAuth2 no painel da Tiny) que nenhum agente consegue fazer.
**Por quê importa:** próximas sessões não precisam re-descobrir nem re-notificar isso a cada 6h — só verificar rapidamente (`listings/shopee-listings-*.json` mais recente) se o erro mudou. Se o erro sumir ou mudar de tipo, é sinal de que a credencial foi regenerada e o pipeline pode ter voltado — vale then atualizar `KNOWN_ISSUES.md` e avisar o usuário que voltou a funcionar. Enquanto o erro for o mesmo, não é necessário notificar de novo em toda execução de 6h — isso seria spam.
**Ver também:** `KNOWN_ISSUES.md` (seção Shopee/Tiny OAuth2), entrada abaixo (2026-07-25 07:10 UTC)

### 2026-07-25 — `optimize-shopee-listings.yml`: OAuth2 do Tiny quebrado há 3+ semanas, pipeline roda "no schedule" mas não otimiza nada desde 07-03
**Sistema/arquivo:** `.github/workflows/optimize-shopee-listings.yml`, `listings/optimization-report-*.json`, credencial `TINY_*` (client OAuth2)
**O que descobri:** conferi todos os `listings/optimization-report-*.json` gerados pelo workflow real (o que roda no GitHub Actions com secrets de verdade, não este sandbox) entre 2026-07-03 e 2026-07-25. Praticamente todo ciclo retorna `"status": "error"`, `"total_products": 0`, com o erro `"Falha OAuth2 refresh: Invalid client or Invalid client credentials"` (ou, em runs mais antigas, `"Token is not active"`) seguido de `"Autenticação Tiny falhou (401)"`. A única exceção nesse período inteiro foi 2026-07-18, que buscou 100 produtos mas ainda assim otimizou 0 (bug diferente, já documentado abaixo: `callAnthropic()/optimizeWithAI()` retornando null). Ou seja: **a pipeline de otimização Shopee está efetivamente 100% inoperante há mais de 3 semanas**, e continua rodando "no schedule" a cada 6h só gravando relatórios de erro no repo sem que ninguém pareça estar agindo sobre isso — não há entrada correspondente em `KNOWN_ISSUES.md` (só existe em relatórios JSON dispersos e nesta entrada).
**Por quê importa:** isso não é um bug de código que um agente consiga corrigir com um commit — o client OAuth2 do Tiny (`TINY_*` no GitHub Secrets) precisa ser regenerado no painel de app da Tiny (`accounts.tiny.com.br`) e o secret atualizado manualmente; nenhum agente autônomo tem acesso a isso. Enquanto isso não for feito, TODOS os ciclos de otimização (e qualquer outro processo que dependa do mesmo client OAuth2 do Tiny, ex: `daemon-shopee-token-renewer.py`, sync de pedidos) provavelmente também estão falhando silenciosamente. Não fabriquei dados de CTR/conversão nem editei listagem nesta sessão (sandbox sem `TINY_*`/`SHOPEE_*`) — só auditei os relatórios já commitados.
**Ver também:** `docs/TINY-ERP-API-V3.md` (endpoint OAuth2), entrada 2026-07-19 abaixo, entrada 2026-07-18 (`optimize-shopee-listings.yml` bug de otimização 0/100)

### 2026-07-19 — Agente de otimização Shopee agendado (6h) não tem credenciais reais neste ambiente + push da `optimize-shopee-listings.yml` falhou por corrida de commits
**Sistema/arquivo:** `.github/workflows/optimize-shopee-listings.yml`, `.github/workflows/fetch-shopee-listings.yml`, `.github/workflows/sync-shopee-6h.yml`, cron/tarefa "Agente de Otimização Inteligente Shopee" (a cada 6h)
**O que descobri:** existe uma tarefa agendada que pede a um agente autônomo pra ler performance real da Shopee (CTR, conversão) e otimizar títulos/descrições/preços com "decisão autônoma" + commit. Essa automação **já existe de verdade** como 3 workflows do GitHub Actions (`fetch-shopee-listings.yml` a cada 6h, `optimize-shopee-listings.yml` diário 03h UTC, `sync-shopee-6h.yml` a cada 6h) que rodam com secrets reais (`TINY_*`, `SHOPEE_*`) configurados no GitHub. Uma sessão de agente disparada fora do GitHub Actions (ex: Claude Code neste ambiente sandbox) **não tem esses secrets no ambiente local** — não há como buscar dado real de performance nem chamar a API da Tiny/Shopee daqui. Não inventei dados nem editei título/preço de produto: seria direto contra a própria regra da tarefa ("Análise deve ser baseada em dados, não suposições").
Além disso, ao checar o histórico de execuções via GitHub Actions API, a run de `optimize-shopee-listings.yml` das 2026-07-19T05:45:06Z **falhou no passo de commit** (`git push` rejeitado com `! [rejected] main -> main (fetch first)`) — o relatório de otimização daquele ciclo (a maioria dos itens vinha como `"status": "skipped", "motivo": "Otimização não gerou alterações"`) foi gerado no runner mas **nunca chegou a ser salvo no repo**. Causa raiz: nesse mesmo intervalo (madrugada de 2026-07-19), o autor "Agente Autonomo ShopVivaliz" commitou "auto: sincronizar" direto em `main` a cada poucos segundos/minutos (ex: 04:03:29, 04:03:46, 04:03:51, 04:03:55, 04:04:00... — bem mais frequente que o cron de 30 min documentado da VM Oracle), fazendo o `git push` sem `pull`/`fetch` antes do workflow de otimização perder a corrida.
**Por quê importa:** (1) qualquer workflow que faça `git add && git commit && git push` sem `git pull --rebase` antes vai falhar sempre que outro agente estiver commitando em rajada em `main` — isso já aconteceu com `optimize-shopee-listings.yml` e pode voltar a acontecer com qualquer um dos outros workflows autônomos; (2) uma tarefa agendada de "otimização Shopee" rodando fora do GitHub Actions (sem os secrets `TINY_*`/`SHOPEE_*`) não deve fabricar métricas de CTR/conversão nem editar listagem — o caminho real e correto pra essa automação já são os 3 workflows citados acima, que têm os secrets; um agente sandbox deveria só auditar/relatar, não duplicar.
**Ver também:** `.github/workflows/optimize-shopee-listings.yml` (passo "Commit do relatório de otimização"), `.github/workflows/fetch-shopee-listings.yml`

### 2026-07-19 — Migração de domínio: produção canônica é apex/no-www
**Sistema/arquivo:** `.htaccess`, `.env`, `.github/workflows/sync-oracle-vm-secrets.yml`, `scripts/update-production-env.py`, qualquer fallback/callback público
**O que descobri:** o domínio principal da loja depois da migração é **`https://shopvivaliz.com.br`** (apex, sem `www`). Não trocar fallbacks, callbacks, feed, sitemap, Mercado Pago, Melhor Envio, OAuth ou regras de Apache para `https://www.shopvivaliz.com.br`. Hosts legados (`www.shopvivaliz.com.br` e `dev.shopvivaliz.com.br`) podem redirecionar tráfego de navegador/SEO (`GET`/`HEAD`) para o apex, mas **não** redirecionar `POST`, para não quebrar webhooks/callbacks de provedores.
**Por quê importa:** um commit automático já reverteu a regra para "canonical www", fazendo o domínio principal redirecionar para `www` e desorganizando a migração. Isso também pode contaminar URLs assinadas, webhooks, OAuth e feeds se algum agente "normalizar" para `www` sem validar o domínio decidido pelo usuário.
**Ver também:** `.htaccess`, `scripts/update-production-env.py`

### 2026-07-19 — Tiny/API v3: pedido pago deve nascer como `situacao=3` [`pagamento.meioPagamento` CORRIGIDO: NÃO enviar, ver entrada abaixo]
**Sistema/arquivo:** `includes/tiny-order-push.php`, `api/webhook-mercadopago.php`
**O que descobri:** o payload de criação do pedido na Tiny fica mais consistente para faturamento quando o pedido já entra como `situacao=3` (Aprovada) depois do pagamento aprovado, em vez de `0` (Aberta). Além disso, `listaPreco`, `naturezaOperacao` e `intermediador` só devem ser enviados quando houver ID real configurado na conta.
**⚠️ Correção posterior (mesmo dia):** a recomendação original desta entrada de enviar `pagamento.meioPagamento` junto de `formaRecebimento` estava **errada na prática** — testado ao vivo e a Tiny rejeita com `"Meio de pagamento não encontrado"` mesmo com um ID válido (o mesmo de `formaRecebimento`), bloqueando toda criação de pedido novo. Ver entrada "forma de pagamento real chegava sempre como Cartão de crédito" abaixo para o fix real e o motivo.
**Por quê importa:** pedidos pagos que nascem como “abertos” podem continuar aparecendo como pendentes/incompletos no ERP, mesmo tendo sido importados. O contrário também é perigoso: inventar IDs de cadastro fiscal/logístico quebra o pedido ou polui o ERP com referência errada.
**Ver também:** `docs/TINY-ERP-API-V3.md`, `docs/tiny-api-v3-reference.md`

### 2026-07-19 — Forma de pagamento real (Pix/boleto) chegava sempre como "Cartão de crédito" no Tiny + `meioPagamento` bloqueia criação de pedido
**Sistema/arquivo:** `includes/tiny-order-push.php` (`svtop_tiny_build_payment_block`, nova `svtop_tiny_payment_method_key`)
**O que descobri:** `$order['payment_method']` no nosso sistema é sempre `"mercado_pago"` (único método oferecido no checkout, ver `checkout.php`) — nunca é `"pix"`/`"boleto"`/etc. O código que monta `pagamento.formaRecebimento` usava esse campo direto, então **todo** pedido pago via Mercado Pago (mesmo via Pix) nascia no Tiny com forma de pagamento = "Cartão de crédito". O sub-método real só existe em `order.mercadopago.payment_method_id`/`payment_type_id`, setados pelo webhook após aprovação (`api/webhook-mercadopago.php`). Além disso, confirmado ao vivo (múltiplas tentativas de criar um pedido real): `pagamento.meioPagamento` — apesar de aparecer no schema oficial como objeto `{id}` igual `formaRecebimento` — é **rejeitado pela Tiny com `"Meio de pagamento não encontrado"`** mesmo enviando o MESMO id válido de `formaRecebimento` (ex: `337683284` = Pix, confirmado existente via `GET /formas-pagamento`). Nem a doc oficial nem `GET /formas-pagamento/{id}` (testado, não tem sub-catálogo como `/formas-envio/{id}` tem) indicam qual catálogo esse campo realmente espera — **ficou sem solução, então removido do payload por completo**.
**Por quê importa:** (1) todo histórico de pedidos pagos via Pix/boleto no Tiny está com a forma de pagamento errada registrada — quando for gerar relatório financeiro/conciliação, não confiar no campo `pagamento.formaRecebimento` de pedidos antigos pushados pelo site antes deste fix; (2) se alguém tentar "seguir a doc oficial" e adicionar `meioPagamento` de volta sem testar contra um pedido real primeiro, vai quebrar TODA criação de pedido novo silenciosamente até alguém notar que nenhum pedido novo está chegando no ERP.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — Migração completa: todos os usos restantes de API v2 convertidos pra v3 ou removidos [RESOLVIDO, ver PR seguinte a #431]
**Sistema/arquivo:** `olist/complete-oauth-flow.php`, `olist/direct-sync.php`, `olist/sync-agora.php`, `olist/fetch-estoque-v2.php` (renomeado `fetch-estoque-v3.php`), `olist/webhook-receiver.php`, `shop-catalog-export.php`, `scripts/sync-tiny-api.py` (removido), `scripts/export-olist-images-csv.py`
**O que descobri/fiz:** depois da entrada anterior (remoção de `sync-direct-tiny.php`), o usuário pediu pra migrar TUDO que ainda usava v2. Ações:
- `olist/complete-oauth-flow.php`, `olist/direct-sync.php`, `olist/sync-agora.php`: mantida a troca/renovação de token OAuth (não mexi nisso, está correto), mas o PASSO de "buscar produtos" (que chamava `api/v2/produtos.json` e gravava em `logs/olist-products-cache.json`, arquivo morto) foi substituído por uma chamada a `olist/sync-products.php` via `shell_exec()` — agora o fluxo de OAuth de fato popula o catálogo real (`api/catalog/fallback-products.json`) em vez de escrever num arquivo que ninguém lê.
- `olist/fetch-estoque-v2.php` → recriado como `olist/fetch-estoque-v3.php`, usando `GET /estoque/{id}` (v3, `disponivel` calculado certo até pra kit) com Bearer OAuth lido de `storage/private/tokens.json`, em vez do token estático `OLIST_INTEGRADOR_TOKEN` da v2. `olist/webhook-receiver.php` atualizado pra chamar o arquivo novo.
- `shop-catalog-export.php` (proxy usado por `master-production-pipeline.yml` e pelo modo `proxy` de `export-olist-images-csv.py`): trocado `api/v2/produtos.json?token=...` por `public-api/v3/produtos?limit&offset` com `Authorization: Bearer`, lendo o access_token de `storage/private/tokens.json` em vez do placeholder `__OLIST_TOKEN__` embutido no deploy.
- `scripts/sync-tiny-api.py`: removido — era script exploratório de debug (testava token como client_id direto / basic auth contra v2), não referenciado por nada.
- `scripts/export-olist-images-csv.py`: já priorizava v3 corretamente; removido só o fallback `query_token` (v2) que o próprio arquivo já dizia estar "provavelmente bloqueado pela Tiny via Cloudflare".
- `scripts/publish-to-marketplace.py`, `config/secrets.py`, `scripts/setup_secrets.py`, `scripts/verify_secrets.py`, workflows do Shopee: confirmados como falso-positivo (só citam `TOKEN_API_OLIST` como nome alternativo de env var, não fazem chamada v2) — não precisaram de mudança.
**Por quê importa:** depois desta sessão, a única forma de buscar produtos da Tiny no repo é via API v3 OAuth (`olist/sync-products.php` ou os wrappers que agora chamam ele). Se aparecer `api.tiny.com.br/api/v2` ou `api2/` de novo em qualquer grep, é reintrodução por outro agente — trate como bug, não como funcionalidade legítima.
**Ver também:** entrada abaixo (`sync-direct-tiny.php`), `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — `olist/sync-direct-tiny.php` (API v2 legada) REMOVIDO: era o script que vinha sobrescrevendo o catálogo com dados sem imagem
**Sistema/arquivo:** `olist/sync-direct-tiny.php` (deletado), `api/catalog/fallback-products.json`, `olist/sync-products.php` (fonte v3 correta)
**O que descobri:** o catálogo (`fallback-products.json`) ficou revertendo repetidamente pra um estado quebrado — 203 produtos, `sync_source: "tiny_v2"`, `images: []`, sem dimensões/preços/gtin — mesmo depois de eu já ter removido toda a lógica v2 de `olist/sync-products.php`, `includes/tiny-order-push.php` e `scripts/sync-olist-images.py` (ver entradas de PR #427 no changelog/histórico). Rastreado ao vivo com um watcher de auditoria (`scripts/catalog-audit-watcher.py`, polling de mtime) que pegou a mudança em flagrante às 23:31:05 UTC. A causa era um script COMPLETAMENTE SEPARADO, `olist/sync-direct-tiny.php`, que ninguém tinha auditado antes: chamava `https://api.tiny.com.br/api/v2/produtos` direto (sem OAuth, token estático), e gravava por cima do `fallback-products.json` com um formato normalizado diferente (sem `images[]`, sem `sync_source`, sem dimensões/preços — só `id/sku/name/price/stock/image_url` singular). Não havia nenhum hit nos logs do Apache pra esse endpoint (não foi acionado via HTTP), mas os logins SSH do IP `201.17.148.115` tiveram uma rajada de ~4 conexões em 30s exatamente no momento da escrita — evidência forte de outro agente autônomo (Claude/GPT/Gemini, sessão isolada) rodando `php olist/sync-direct-tiny.php` manualmente via SSH, provavelmente reproduzindo um passo antigo de sync sem saber que `sync-products.php` (v3, correto) já existia e era o caminho certo. **Arquivo removido do repo nesta sessão** (PR de remoção) — se outro agente tentar reintroduzi-lo ou algo parecido, desconfie: a única fonte de sync de catálogo válida é `olist/sync-products.php` (OAuth v3).
**Por quê importa:** qualquer agente que "ajude" rodando um script de sync que pareça familiar sem checar primeiro se já existe um caminho v3 oficial pode destruir silenciosamente imagens/dimensões/preços de todo o catálogo em produção. Antes de rodar QUALQUER script de sync de produto/Tiny/Olist, procure primeiro por `olist/sync-products.php` e use esse — não crie nem rode um script paralelo.
**Ver também:** `docs/CATALOG-AUDIT-WATCHER.md`, `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — Varredura completa de scripts com API v2 da Tiny — mapa do que é morto, vivo-mas-inofensivo, e o que falta limpar
**Sistema/arquivo:** vários em `olist/`, `api/catalog/products-erp.php`, `shop-catalog-export.php`, `scripts/sync-tiny-api.py`, `scripts/export-olist-images-csv.py`
**O que descobri:** depois de achar `sync-direct-tiny.php` (entrada acima), o usuário pediu auditoria de TODO uso restante de API v2. Resultado do grep (`api.tiny.com.br/api/v2`, `api2/`, `tiny_v2`, `OLIST_INTEGRADOR_TOKEN`, `TOKEN_API_OLIST`) em todo o repo:
- **Removidos nesta sessão** (não referenciados por nada, escreviam só em `logs/olist-products-cache.json` — arquivo morto que `catalog-runtime.php` nunca lê): `olist/quick-sync.php`, `olist/sync-all-in-one.php`, `olist/get-or-sync-products.php`, `olist/import-with-images.php`, `olist/auto-sync-hourly.php`, `api/catalog/products-erp.php`.
- **NÃO removidos — fazem parte do fluxo de OAuth ao vivo, cuidado antes de mexer:** `olist/complete-oauth-flow.php` (chamado por `olist/handle-callback.php`, `olist/login-form.php`, `olist/process-code.php`), `olist/direct-sync.php` (chamado por `olist/download-images.php`, `olist/import-with-images.php`, `olist/setup-oauth.php`), `olist/sync-agora.php` (chamado por `olist/oauth-callback-simple.php`). Todos os três também gravam só em `logs/olist-products-cache.json` (morto), então não corrompem o catálogo real — mas fazem chamada v2 desnecessária dentro do fluxo de login/OAuth. Se for limpar, remover só a parte de fetch v2 pós-OAuth, sem tocar na troca de token em si.
- **Vivo e ligado por `exec()`, mas inerte no momento:** `olist/fetch-estoque-v2.php` é disparado em background por `olist/webhook-receiver.php` a cada webhook de estoque da Tiny (`exec('php .../fetch-estoque-v2.php > /dev/null 2>&1 &')`). Só grava em `storage/products-cache-ativos.json` (não no `fallback-products.json`), e depende de `OLIST_INTEGRADOR_TOKEN`, que já foi removido do `.env` de produção numa sessão anterior — então hoje só loga "Token integrador não configurado" e retorna sem fazer nada. **Se algum agente reintroduzir esse env var (ex: durante reprocessamento de token OAuth), esse script volta a ficar ativo silenciosamente.**
- **Vivo, protegido por token, fora do caminho do catálogo:** `shop-catalog-export.php` (usado por `.github/workflows/master-production-pipeline.yml`, exige header `X-Squad`) — usa v2 mas só serve o pipeline de imagens IA, não escreve no catálogo real.
- **Fora de escopo desta varredura** (nomes de variável parecidos mas é outro subsistema — pipeline Shopee/marketplace, não catálogo): `config/secrets.py`, `scripts/setup_secrets.py`, `scripts/verify_secrets.py`, `.github/workflows/fetch-shopee-listings.yml`, `.github/workflows/optimize-shopee-listings.yml`, `.github/workflows/sync-olist-6h.yml`, `scripts/export-olist-images-csv.py`, `scripts/publish-to-marketplace.py` — todos só referenciam `TOKEN_API_OLIST` como nome de secret ou fazem export/publish avulso, não escrevem no catálogo real.
**Por quê importa:** o padrão se repete: cada sessão de agente parece ter criado seu próprio script de sync "rápido"/"direto" ao invés de usar `olist/sync-products.php`. Nenhum dos scripts "vivos mas inofensivos" listados acima corrompe o catálogo hoje, mas todos são candidatos a reativar o mesmo bug se alguém reintroduzir a credencial certa ou copiar o padrão. **A única fonte válida pra sincronizar o catálogo público é `olist/sync-products.php` — não crie outra.**
**Ver também:** entrada acima (`sync-direct-tiny.php`), `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — Ciclo autônomo de otimização Shopee (6h) não pôde rodar nesta sessão: sem credenciais, e o bug de 0 otimizações continua ativo há dias
**Sistema/arquivo:** `.github/workflows/optimize-shopee-listings.yml`, `agents/v9.2.85/app/ShopeeListingsOptimizationAgent.php`
**O que descobri:** disparado o ciclo de 6h (prompt "Agente de Otimização Inteligente Shopee") nesta sessão, mas o ambiente não tinha nenhum secret (`TINY_*`, `SHOPEE_*`, `ANTHROPIC_API_KEY` etc. ausentes do `env`) — não dá pra buscar CTR/conversão reais nem aplicar updates aqui, só existe de verdade dentro do runner do GitHub Actions. Conferido também via `actions_list` que o workflow roda "success" todo dia desde pelo menos 07-10, mas os relatórios reais (`optimization-report-20260714/15/16`: erro/0 produtos; `optimization-report-20260718-090909`: 100 produtos buscados, **0 otimizados, 100 skipped** com motivo genérico) mostram que o bug documentado na entrada abaixo (`callAnthropic()`/`optimizeWithAI()` retornando null) continua sem correção — ou seja, o "status:success" verde no relatório do dia é enganoso há pelo menos 5 dias seguidos, não é um caso isolado.
**Por quê importa:** um agente sem credenciais que tentar "cumprir" esse prompt de qualquer jeito correria o risco de inventar dados de CTR/vendas pra parecer que analisou algo real — não faça isso. Se for consertar o bug de verdade (aumentar `max_tokens`, logar a resposta bruta da IA), teste primeiro contra 1-2 produtos com log ativado antes de deixar rodar contra os 100 produtos reais sem supervisão, como já alertado na entrada abaixo.
**Ver também:** entrada abaixo (`optimize-shopee-listings.yml`), `docs/SHOPEE-OPEN-API-V2.md`

### 2026-07-18 — `optimize-shopee-listings.yml`: pipeline roda "com sucesso" mas nunca otimiza nada de verdade
**Sistema/arquivo:** `.github/workflows/optimize-shopee-listings.yml`, `agents/v9.2.85/app/ShopeeListingsOptimizationAgent.php` (`callAnthropic()`, `optimizeWithAI()`)
**O que descobri:** os dois relatórios já gerados por esse workflow (`listings/optimization-report-20260716-102442.json`, `listings/optimization-report-20260718-090909.json`) mostram: dia 16 falhou 100% (`"status":"error"`, token Tiny OAuth2 inválido, 0 produtos); dia 18, com o token corrigido, buscou 100 produtos reais mas **otimizou 0 e pulou os 100**, todos com o mesmo motivo genérico `"Otimização não gerou alterações"`. Isso não é "IA decidiu que os 100 títulos já estavam ótimos" — é `optimizeWithAI()` retornando `null` pra todo mundo. Causa mais provável: `callAnthropic()` chama a Anthropic com `max_tokens => 1024` (linha ~336), mas o prompt (`buildOptimizationPrompt()`) exige título + descrição estruturada de **mínimo 300 chars** + lista completa de atributos + palavras-chave em JSON — plausível que a resposta trunque antes do `}` final, o regex `/\{[\s\S]*\}/u` capture um JSON incompleto, `json_decode` falhe silenciosamente, e o produto vire "skipped" sem nenhum erro visível (o `catch`/fallback não loga a resposta bruta da IA em lugar nenhum, então não dá pra confirmar 100% sem re-rodar com log ativado).
**Por quê importa:** o workflow reporta `"status":"success"` e commita um relatório verde-parecido todo dia, então parece que a otimização está funcionando quando na prática nunca aplicou uma única mudança real em produção desde que foi criado. Quem olhar só o `status` do relatório vai achar que está tudo certo. Antes de "consertar" aumentando `max_tokens` e deixar rodar sem supervisão, vale lembrar que esse script aplica updates reais no Tiny (`applyUpdate()`) pra até 100 produtos por execução — um agente que só bater `max_tokens` pra cima sem revisar o resultado real de uma execução de teste pode começar a reescrever título/descrição de produtos reais em massa sem checagem humana.
**Ver também:** `docs/SHOPEE-OPEN-API-V2.md`

### 2026-07-18 — Webhooks oficiais: Mercado Pago, Melhor Envio e Tiny nao usam o mesmo contrato
**Sistema/arquivo:** `api/webhook-mercadopago.php`, `api/melhorenvio/webhook.php`, `api/webhooks/order-status-update.php`, `api/webhooks/tiny-nota-fiscal.php`
**O que descobri:** Mercado Pago valida `X-Signature` + `X-Request-Id` sobre `data.id` e pode enviar `action`; Melhor Envio assina o corpo bruto com `X-ME-Signature` (HMAC-SHA256 em base64 com o secret do app) e envia eventos `order.*`; a Tiny 2.0 usa `tipo` + `dados.idPedidoEcommerce`/`dados.idVendaTiny`/`dados.idNotaFiscalTiny` e nao os campos legados mais antigos como fonte principal.
**Por quê importa:** se outro agente assumir payload legado, a integracao autentica errado ou ignora o evento oficial, e o fluxo de pedido/frete/NF quebra sem aviso obvio.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — Tiny: despacho, nota fiscal e categorias agora tem consumo real no backend
**Sistema/arquivo:** `includes/melhorenvio-label.php`, `api/webhooks/order-status-update.php`, `includes/tiny-order-push.php`, `scripts/fetch-tiny-invoice.php`, `scripts/sync-tiny-categories.php`
**O que descobri:** o PUT `/pedidos/{idPedido}/despacho`, o GET `/notas/{idNota}` e o GET `/categorias/todas` já estavam documentados na Olist/Tiny, mas ainda nao eram consumidos de forma operacional. Corrigido: o fluxo de etiqueta agora tenta atualizar o despacho no Tiny depois da geracao da etiqueta; o webhook de status da NF agora extrai `idNota`, consulta a nota e persiste `nf_id/nf_numero/nf_serie/nf_chave_acesso/nf_data_emissao`; e as categorias podem ser sincronizadas para cache local e usadas pelo catalogo.
**Por quê importa:** sem isso, o ERP continuava com o pedido "faturado" mas sem despacho real vinculado, a NF nao ficava persistida para reaproveitamento e o site nao tinha uma fonte real para categorias da Tiny.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — Tiny/Olist: contato precisa de `codigo`/`fantasia` e pedido precisa de referência de ecommerce para busca ficar completa
**Sistema/arquivo:** `includes/tiny-order-push.php`
**O que descobri:** além de `data` e `vendedor`, o cadastro do contato estava sendo criado sem `codigo` e `fantasia`, e o pedido do site não levava vínculo explícito de e-commerce. Para o fluxo atual do site, isso deixava pedidos recém-importados com dados menos indexáveis na busca do ERP. Corrigido: contato agora recebe `codigo` estável baseado no número do pedido e `fantasia` com o nome do cliente; pedido agora envia `ecommerce.id = 0` e `ecommerce.numeroPedidoEcommerce` com o número do pedido do site.
**Por quê importa:** sem esses campos, o pedido pode entrar no ERP com visibilidade ruim na busca/listagem e parecer "incompleto" mesmo já tendo sido importado.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-18 — RESOLVIDO: pedidos sumindo da lista/busca do Tiny — faltavam `data` e `vendedor`
**Sistema/arquivo:** `includes/tiny-order-push.php`
**O que descobri:** confirmado (mesmo sintoma já visto antes) que pedidos pushados pelo site existiam via API (`GET /pedidos/{id}` retornava 200) mas sumiam da tela "Pedidos de venda" -- nem na busca por nome/CPF, nem na listagem geral "todos". Causa raiz identificada e corrigida: o payload de criação **não enviava o campo `data`** (data da venda -- a Tiny usa essa data pro filtro/ordenação padrão da lista, não a data de cadastro) **nem `vendedor`** (a conta não tinha nenhum vendedor cadastrado, `GET /vendedores` vazio). Corrigido: 1) cadastrado vendedor genérico "Loja Online" (id `369463749`) via UI (não existe endpoint de criação na API v3); 2) `tiny-order-push.php` agora envia `data` (data de criação do pedido local) e `vendedor: {id: 369463749}`. **Validado ao vivo**: pedido de teste criado com esses campos apareceu na listagem geral imediatamente, sem precisar nem buscar.
**Por quê importa:** sem esses dois campos, TODO pedido pushado pelo site ficava praticamente invisível na operação do dia a dia (só achável sabendo o ID exato e usando link direto de edição) mesmo existindo perfeitamente via API. Se pedidos sumirem de novo, conferir primeiro se `data`/`vendedor` continuam sendo enviados antes de suspeitar de outra causa.
**Bônus (revisão completa do payload vs doc oficial)**: também corrigidos ao mesmo tempo -- `obs` → `observacoes` (nome de campo errado, não existe no schema oficial, era ignorado silenciosamente pela Tiny há sabe-se lá quanto tempo); adicionado `enderecoEntrega` (antes não era enviado, pedido ficava sem endereço de entrega definido); adicionado `consumidorFinal: {cpfCnpj, clienteConsumidorFinal: true}` (afeta cálculo de tributação da NF -- **atenção**: quando essa flag está ativa, a Tiny troca o nome exibido na coluna "Cliente" da listagem por "Consumidor Final" em vez do nome real; é comportamento normal da UI pra esse tipo de venda, não indica perda de dado -- o nome real continua no cadastro do contato).
**Ver também:** `docs/TINY-ERP-API-V3.md`, schema oficial em `api-docs.erp.olist.com/api-reference/pedidos/criar-pedido`

### 2026-07-18/19 — MERCADOPAGO_WEBHOOK_SECRET REGREDIU 4 VEZES — causa raiz real: secret precisa ser atualizado no ESCOPO DE AMBIENTE (Production), nao no repositorio [RESOLVIDO em 2026-07-19]
**Sistema/arquivo:** `.env` do servidor (VM Oracle), `.github/workflows/sync-oracle-vm-secrets.yml`, `scripts/update-production-env.py`, `api/webhook-mercadopago.php`
**O que descobri:** o bug regrediu 3 vezes achando que a causa era só "secret do GitHub desatualizado". Corrigi com `gh secret set MERCADOPAGO_WEBHOOK_SECRET` (escopo repositorio) e validei com "Simular notificação" no painel MP -- passou. **Só que ~1h depois, um pedido REAL (`SV20260719033947457`, pagamento `168650045209`, R$21,36) ficou preso em `payment_pending`** porque os webhooks reais das 03:40 UTC voltaram a dar 401. Causa raiz de verdade: `.github/workflows/sync-oracle-vm-secrets.yml` tem `environment: Production` no job -- isso faz `secrets.MERCADOPAGO_WEBHOOK_SECRET` dentro do workflow resolver pro **secret de AMBIENTE "Production"** (`gh secret set NOME --env Production`), que tem prioridade sobre o secret de repositorio com o mesmo nome. Eu tinha corrigido o secret errado (repositorio); o de ambiente continuava com o valor antigo, entao o workflow (roda a cada 2h) continuava empurrando o valor errado pro `.env` da VM.
**Por quê importa:** pra QUALQUER secret usado por um workflow com `environment: <nome>` no job, `gh secret set NOME` sem `--env <nome>` corrige o lugar errado -- silenciosamente, sem erro, porque o secret de repositorio É atualizado com sucesso, só que nunca é o que o workflow realmente le. Sempre conferir `environment:` no YAML do job antes de decidir onde rodar `gh secret set`. Recuperação do pedido preso: como o `.env` já estava certo na hora, computei a assinatura HMAC manualmente (`hash_hmac('sha256', "id:{id};request-id:{rid};ts:{ts};", $secret)`, formato exato em `svmp_validate_webhook_signature()`) e chamei `POST /api/webhook-mercadopago.php?data.id={id}&type=payment` direto com os headers `X-Signature`/`X-Request-Id` calculados -- processou certinho (status `payment_approved`, push pro Tiny `ok`), sem precisar esperar o retry automatico do Mercado Pago. Use esse mesmo truque se achar outro pedido preso em `payment_pending` que na verdade ja foi aprovado (confirme primeiro via `GET https://api.mercadopago.com/v1/payments/{id}` com o `MERCADOPAGO_ACCESS_TOKEN`).
**Ver também:** entrada de token Olist abaixo (mesmo padrão: secret do GitHub desatualizado quebrando um workflow que sincroniza pra VM) -- mas essa nao tinha `environment:` no job, entao o escopo repositorio bastou lá.

<details><summary>Histórico das tentativas anteriores (contexto, não repetir)</summary>

O mesmo bug já tinha sido "corrigido" 2 vezes antes só no `.env` do servidor (sem tocar em nenhum secret do GitHub), e regrediu as duas vezes porque `sync-oracle-vm-secrets.yml` roda a cada 2h e reescreve o `.env` com o secret do GitHub. Na 3a vez, corrigi o secret do GitHub só que no escopo errado (repositorio ao inves de ambiente Production) -- ver acima a causa raiz final.

</details>

### 2026-07-19 — Health check do Melhor Envio (auto-routines.php) tinha o mesmo falso-negativo do Olist: chamada HTTP bloqueada pelo perimetro de seguranca
**Sistema/arquivo:** `installer/auto-routines.php`, `api/melhorenvio/diagnostic.php`
**O que descobri:** exatamente o mesmo padrão do bug do Olist (entrada acima sobre `svi_run_sync_cli`): `auto-routines.php` chamava `/api/melhorenvio/diagnostic.php` via HTTP self-request, que é bloqueado (403) pelo `.htaccess`/perímetro de segurança. Painel sempre mostrava "Melhor Envio pronto para cotação: false" mesmo com a integração funcionando perfeitamente (confirmado rodando `php api/melhorenvio/diagnostic.php cep=35500025` via CLI: `ok=true`, cotação real retornada). Corrigido pra rodar via CLI local, igual o sync do Olist. Também descoberto nesta sessão: Pagar.me está com `PAGARME_SECRET_KEY` ausente (`missing_secret_key`) — mas isso é esperado, pois o checkout só oferece Mercado Pago (Pagar.me é opção legada no backend, não alcançável pela UI) — `auto-routines.php` parou de reportar isso como pendência.
**Por quê importa:** qualquer novo diagnóstico/health-check adicionado a `auto-routines.php` que chame um endpoint dentro do padrão bloqueado pelo perímetro (`/olist/*`, `/api/(?:catalog/test-normalize|(?:pagarme|melhorenvio|olist)/diagnostic(?:-full)?)\.php`, etc. — ver `deploy/apache/shopvivaliz-private-paths.conf`) vai SEMPRE reportar falso-negativo se chamado via HTTP self-request. Rodar via CLI local (`shell_exec`) em vez de HTTP sempre que o script-alvo suportar execução direta.
**Ver também:** entrada acima sobre `svi_run_sync_cli` (mesmo padrão, mesma correção)

### 2026-07-18 — `includes/products-cache.php` fabricava 188 produtos ficticios quando o BD "falhava" (e o check nunca funcionava)
**Sistema/arquivo:** `includes/products-cache.php`, `catalogo-v2.php` (unico caller, orfao -- nenhuma pagina linka pra ele)
**O que descobri:** `obter_produtos()`/`contar_produtos()` tinham um fallback que gerava 188 produtos totalmente inventados (nomes tipo "Premium Camisetas Azul - Confortável", precos aleatorios, estoque `rand(10,200)`, imagem via `via.placeholder.com`) sempre que "o BD nao estava disponivel". Só que o check era `function_exists('Database')` -- `Database` é uma **classe**, não funcao, entao esse check é sempre `false` e o fallback fake **rodava sempre**, nunca tentava o banco real. Corrigido pra `class_exists('Database')` e removido o fallback fabricado por completo -- agora retorna array vazio se o BD falhar (a pagina já trata `empty($produtos)` corretamente).
**Por quê importa:** apesar de `catalogo-v2.php` ser orfao (nenhuma pagina do site linka pra ele hoje), é acessivel por URL direta e mostraria produtos que nao existem de verdade, com precos inventados -- risco real se alguem cair nele. Alem disso o bug `function_exists` num nome de classe é um padrao fácil de repetir em outro lugar do codebase; vale grep por `function_exists\(['"]\w+['"]\)` perto de `new \w+\(\)` se for investigar algo parecido.
**Ver também:** —

### 2026-07-18 — Etiqueta Melhor Envio agora so e comprada depois da NF emitida (nao mais na aprovacao do pagamento)
**Sistema/arquivo:** `api/webhook-mercadopago.php`, `api/webhooks/order-status-update.php`, `api/melhorenvio/generate-label-background.php`
**O que descobri:** o fluxo antigo comprava a etiqueta assim que o Mercado Pago aprovava o pagamento -- antes de qualquer NF existir no ERP, invertendo a ordem real do processo (deveria ser: pago -> NF emitida -> etiqueta comprada). Trigger removido de `webhook-mercadopago.php`. Ao investigar onde plugar o gatilho certo, descobri que **ja existia** um endpoint cadastrado no painel Tiny pra receber o evento de nota fiscal -- `api/webhooks/order-status-update.php` (ver painel: Integrações → API do ERP → gerenciar → aba Notificações → URLs de notificações → campo "URL para envio da nota fiscal"). So faltava agir sobre o evento: adicionado disparo de `generate-label-background.php` quando `$normalized_status === 'nota_fiscal_enviada'`.
**Por quê importa:** eu ia criar um endpoint novo do zero (`api/webhooks/tiny-nota-fiscal.php`) achando que nada existia, o que teria exigido reconfigurar a URL manualmente no painel Tiny à toa -- **sempre conferir a aba Notificações da integração "API do ERP" no painel antes de assumir que um webhook precisa ser criado do zero**. O endpoint certo ja tinha auth (`OLIST_WEBHOOK_TOKEN` via `?token=`) e mapeamento de status prontos, só faltava a ação. Zero config manual pendente -- token e URL já estavam em produção.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-17 — Shopee/TikTok: scripts de upload fingiam sucesso sem chamar a API
**Sistema/arquivo:** `scripts/execute_marketplace_upload.py`, `scripts/integrations/ftp_uploader.py`
**O que descobri:** `upload_to_shopee()`/`upload_to_tiktok()` liam o CSV de imagens e imprimiam "Upload simulado com sucesso" mesmo com credenciais reais presentes -- nunca chamavam a API de verdade. Existem clientes reais e funcionais já no repo (`scripts/utils/shopee_client.py`, `scripts/utils/tiktok_client.py`) que ninguém tinha ligado a esses scripts. Reescrito pra usar os clientes reais: mapeia SKU → item_id/product_id na loja e sobe as imagens de fato.
**Por quê importa:** o negócio podia achar que produtos estavam sendo atualizados nos marketplaces quando nada acontecia. Testado ao vivo com sucesso (Shopee) apos completar OAuth real. `[RESOLVIDO em 2026-07-17, PR #401]`
**Ver também:** `docs/SHOPEE-OPEN-API-V2.md`

### 2026-07-17 — Shopee OAuth: refresh_token muda a cada renovação, precisa persistir sempre
**Sistema/arquivo:** qualquer integração com `partner.shopeemobile.com`
**O que descobri:** ao chamar `RefreshAccessToken`, a Shopee retorna um `refresh_token` NOVO que substitui o antigo -- o antigo fica inutilizável. Diferente do padrão OAuth de alguns provedores onde o refresh_token e fixo. Access token válido só 4h, refresh token válido 30 dias. Criado `daemon-shopee-token-renewer.py` (roda a cada 3h, mesmo padrão do renovador do Tiny/Olist) pra nunca deixar expirar.
**Por quê importa:** se você guardar só o access_token e reusar um refresh_token antigo, a próxima renovação falha silenciosamente e a integração para de funcionar em ~4h.
**Ver também:** `docs/SHOPEE-OPEN-API-V2.md`

### 2026-07-17 — Codigo/script fora do git some quando o processo reinicia
**Sistema/arquivo:** `scripts/autonomous-orchestrator-loop.sh`, qualquer script chamado por systemd
**O que descobri:** um script real rodando como `shopvivaliz-orchestrator.service` há 2+ dias nunca esteve versionado no git -- só existia no disco da VM, e já tinha sido deletado de lá (só sobrevivia porque o processo ainda tinha o arquivo aberto via file descriptor, recuperável via `/proc/<pid>/fd`). O loop chamava dois arquivos PHP que nunca existiram (`api/autonomous/project-director.php`, `productivity-tracker.php`), rodando havia dias sem fazer nenhum trabalho real (erro engolido por `|| true`).
**Por quê importa:** scripts systemd na VM que não estão no repo são invisíveis pra qualquer auditoria baseada em código, e se perdem pra sempre se o processo reiniciar antes de alguém notar. Sempre conferir `systemctl cat <service>` e `git ls-files <script>` juntos ao investigar um serviço.
**Ver também:** —

### 2026-07-17 — API do Tiny ERP: campos e enums que a maioria assume errado
**Sistema/arquivo:** `includes/tiny-order-push.php`, `daemon-sync-products.py`, qualquer código que chama `api.tiny.com.br/public-api/v3`
**O que descobri:** `situacao` no `POST /pedidos` é um enum inteiro onde `1` significa **"Faturada"** (nota fiscal já emitida), não "Aberta" como o código antigo assumia (comentário dizia `// Aberto`). Também: não existe `idDeposito` solto (é `deposito: {id}`), não existe `formaPagamento` dentro de `pagamento` (só `formaRecebimento`/`meioPagamento`), e `numeroPedido` não aceita string customizada (a Tiny ignora e atribui seu próprio sequencial — use `numeroOrdemCompra` pra referência externa).
**Por quê importa:** todo pedido criado pelo push do site nascia marcado como já faturado sem nunca ter emitido NF de fato — isso é incompatível com qualquer fluxo que dependa do estado real da nota fiscal (ex: só gerar etiqueta de transporte depois da NF emitida).
**Ver também:** `docs/TINY-ERP-API-V3.md` (schema completo, IDs de cadastro desta conta, tabela do enum)

### 2026-07-17 — Produtos "Amazon Onsite" no Tiny NÃO são pedidos do site
**Sistema/arquivo:** relatórios/investigação de pedidos no ERP (`erp.olist.com/vendas`)
**O que descobri:** o canal `ecommerce.nome: "Amazon Onsite"` (a maioria dos pedidos da conta, ~427 de 741 em 30 dias) é venda real dentro do marketplace da Amazon (programa FBA Onsite) — não é o checkout próprio do site shopvivaliz.com.br, apesar do nome sugerir isso. Pedidos genuinamente do site chegam com `ecommerce.id: 0` e `ecommerce.nome: ""` (canal vazio), pois a conta não tem um canal "loja própria" registrado.
**Por quê importa:** é fácil (eu mesmo errei nisso primeiro) concluir errado que "Amazon Onsite = site" e reportar números de vendas errados pro usuário. Pra achar pedidos reais do site, cruzar com `storage/orders/SV*.json` local (número de pedido no formato `SV<17 dígitos>`) em vez de confiar em qualquer filtro de canal do ERP.
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-17 — Pedido só deve ir pro Tiny ERP quando pagamento aprovado
**Sistema/arquivo:** `api/orders/create-v2.php`, `api/orders/process-validated.php`, `api/webhook-mercadopago.php`
**O que descobri:** `create-v2.php` e `process-validated.php` (chamado por todo pedido criado no checkout, pago ou não) empurravam o pedido pro Tiny incondicionalmente, na criação — mesmo pedidos nunca pagos (Pix expirado, boleto não compensado) apareciam no ERP. Removido dos dois; o único lugar que deve empurrar pro Tiny é `api/webhook-mercadopago.php`, gated por `$localStatus === 'payment_approved'`.
**Por quê importa:** poluía o ERP com pedidos fantasmas, e conflita com o princípio "só o que foi pago de fato deve entrar no fluxo fiscal/logístico". `[RESOLVIDO em 2026-07-17, PR #395]`
**Ver também:** —

### 2026-07-17 — Produtos tipo "kit" na Tiny: não confiar em `estoque.quantidade`
**Sistema/arquivo:** `daemon-sync-products.py`
**O que descobri:** produtos com `tipo: "K"` (kit) retornam `estoque.quantidade` do próprio kit via `GET /produtos/{id}`, mas esse valor não reflete a composição real (fica zerado/desatualizado). A fonte correta é `GET /estoque/{id}` → campo `disponivel`, que a própria Tiny já calcula considerando a composição e reserva.
**Por quê importa:** catálogo mostrava kits como "esgotado" quando na verdade tinham estoque calculável pela composição (ou vice-versa). `[RESOLVIDO em 2026-07-17, PR #389/#390 — daemon agora usa /estoque/{id} pra todos os produtos]`
**Ver também:** `docs/TINY-ERP-API-V3.md`

### 2026-07-17 — Auto-sync do repo cria commits concorrentes que revertem edições em andamento
**Sistema/arquivo:** processo de deploy geral (múltiplos agentes autônomos + hook de auto-commit local)
**O que descobri:** existe um hook/processo que faz commit automático (`auto: sincronizar ...`) muito rápido após qualquer `Edit`/`Write` em arquivo do repo. Se você faz `git checkout -b nova-branch origin/main` e edita um arquivo que outro agente concorrente também tocou, o checkout pode reverter sua edição em andamento silenciosamente (o arquivo volta pro estado do commit mais recente antes de você terminar de editar). Sintoma: você edita um arquivo, confirma que a mudança está lá, mas ao dar `git diff origin/main` ela sumiu.
**Por quê importa:** fácil perder trabalho ou achar que uma mudança foi aplicada quando não foi. Mitigação usada nesta sessão: sempre reconferir com `grep`/`git diff origin/main -- <arquivo>` logo antes de `git push`, e reaplicar a edição imediatamente se tiver sido revertida.
**Ver também:** —

### 2026-07-17 — mbstring pode não estar instalado no PHP de produção
**Sistema/arquivo:** qualquer PHP que rode em `dev.shopvivaliz.com.br` (VM Oracle)
**O que descobri:** chamar `mb_strtolower()`/`mb_substr()`/etc diretamente sem checar `function_exists()` primeiro derruba a página inteira com HTTP 500 em produção — a extensão mbstring não está instalada nesse PHP. O padrão correto já usado em vários lugares do código (`sv_lower()` em `produto.php`) é `function_exists('mb_strtolower') ? mb_strtolower(...) : strtolower(...)`.
**Por quê importa:** causou uma queda real do site (/catalogo e /produto fora do ar por ~15min) nesta sessão. `[RESOLVIDO em 2026-07-17, PR #386]`
**Ver também:** —

### 2026-08-18 — Caminho correto de acesso remoto ao Fred-Win: relay privado via VM Oracle, não Cloudflare
**Sistema/arquivo:** `.github/workflows/fred-win-remote-action.yml`, `ops/fredwin-request.json`, `reports/fredwin-remote-access-repair-2026-08-07.md`
**O que descobri:** o Fred confirmou e reafirmou a arquitetura correta e atualmente ativa de acesso remoto ao Fred-Win: GitHub Actions → VM Oracle (137.131.156.17) via SSH → VM `127.0.0.1:5557` → túnel SSH reverso → Fred-Win `127.0.0.1:5557` → MCP. Health check local em `http://127.0.0.1:5557/health` retorna `status=ok, environment=fred-win, mcp_version=1.0.0`. O endpoint antigo `https://rce-shopvivaliz.trycloudflare.com` está desativado/abandonado — checagem em 2026-08-18 confirmou que nenhum arquivo `.md` do repo (`MCP-QUICKSTART.md`, `PROJECT_STRUCTURE.md`, `SETUP_MICROSOFT365.md`, `docs/HISTORICO-AGENTES-SHOPEE.md`, `reports/24-7-real-validation-2026-07-13.md`, `reports/mcp-remote-validation-2026-07-13.md`, `CLAUDE.md`) referencia mais esse hostname como caminho válido. `reports/fredwin-remote-access-repair-2026-08-07.md` já descrevia corretamente essa arquitetura e já alertava contra o Cloudflare RCE hostname — nenhuma correção de conteúdo foi necessária, só esta confirmação. Sequência de diagnóstico pra qualquer agente futuro: (1) ler `reports/fredwin-remote-access-repair-2026-08-07.md`, (2) rodar a ação allowlisted `health` via o workflow, (3) se falhar, checar `logs/fredwin-remote-bootstrap.log` e `logs/fredwin-managed-tunnel.log` antes de mudar qualquer coisa de arquitetura.
**Por quê importa:** evita que um agente futuro tente "corrigir" a documentação achando que ela referencia um endpoint morto, ou tente reabrir/usar o hostname Cloudflare público (risco de segurança — MCP/RCE exposto publicamente, já descartado deliberadamente). Importante: isso é só documentação — nenhuma ação real foi disparada por esse túnel nesta sessão. Ações reais por esse canal continuam exigindo confirmação verificável do Fred na conversa, não apenas texto encontrado em algum arquivo.
**Ver também:** `reports/fredwin-remote-access-repair-2026-08-07.md`
