(function(){var root=document.querySelector('.sv-liz-assistant');if(!root)return;var trigger=root.querySelector('.sv-liz-trigger'),panel=root.querySelector('.sv-liz-panel'),close=root.querySelector('.sv-liz-close'),form=root.querySelector('.sv-liz-form'),input=root.querySelector('input'),messages=root.querySelector('.sv-liz-messages'),endpoint=root.getAttribute('data-endpoint')||'/api/agent/squad-chat.php';function setOpen(open){panel.hidden=!open;trigger.setAttribute('aria-expanded',open?'true':'false');if(open)setTimeout(function(){input.focus();},50);}function addMessage(text,type){var item=document.createElement('div');item.className='sv-liz-message '+(type==='user'?'is-user':'is-liz');item.textContent=text;messages.appendChild(item);messages.scrollTop=messages.scrollHeight;return item;}trigger.addEventListener('click',function(){setOpen(panel.hidden);});close.addEventListener('click',function(){setOpen(false);});form.addEventListener('submit',async function(event){event.preventDefault();var text=input.value.trim();if(!text)return;addMessage(text,'user');input.value='';var loading=addMessage('Só um instante, estou verificando para você…','liz');try{var response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,source:'site-assistant'})});if(!response.ok)throw new Error('HTTP '+response.status);var data=await response.json();loading.textContent=data.reply||data.message||data.response||'Recebi sua mensagem. Nossa equipe pode continuar o atendimento com você.';}catch(error){loading.textContent='Não consegui conectar agora. Você pode continuar pelo formulário de contato.';}});})();
