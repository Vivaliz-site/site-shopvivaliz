{
  "action": "terminal",
  "shell": "powershell",
  "timeout": 240,
  "command": "$ErrorActionPreference='Continue'; $sshCmd=Get-Command ssh -ErrorAction Stop; $sshPath=$sshCmd.Source; $ver=(cmd /c \"\\\"$sshPath\\\" -V 2>&1\" | Out-String).Trim(); $svc=Get-Service ssh-agent -ErrorAction SilentlyContinue; if($svc -and $svc.Status -eq 'Stopped' -and $svc.StartType -ne 'Disabled'){ try { Start-Service ssh-agent -ErrorAction Stop } catch {} ; $svc=Get-Service ssh-agent -ErrorAction SilentlyContinue }; $cfg=Join-Path $HOME '.ssh\\config'; $entries=@(); if(Test-Path $cfg){ $cur=$null; foreach($raw in Get-Content $cfg){ $t=$raw.Trim(); if(!$t -or $t.StartsWith('#')){continue}; if($t.StartsWith('Host ')){ $h=$t.Substring(5).Trim(); if($h -notmatch '[*?]' -and $h -notmatch '\\s'){ $cur=[ordered]@{Alias=$h;HostName='';User=''}; $entries += [pscustomobject]$cur } else { $cur=$null } } elseif($cur -and $t.StartsWith('HostName ')){ $cur.HostName=$t.Substring(9).Trim() } elseif($cur -and $t.StartsWith('User ')){ $cur.User=$t.Substring(5).Trim() } } }; $target=$entries | Where-Object { $_.HostName -eq '137.131.156.17' } | Select-Object -First 1; if(!$target){ $target=$entries | Where-Object { $_.Alias -match '(?i)shopvivaliz|vivaliz|oracle|prod' } | Select-Object -First 1 }; if(!$target){ Write-Output 'TERMINAL_READY=NO'; Write-Output 'SSH_HOST_ALIAS='; Write-Output 'ERROR=No matching existing SSH alias found'; exit 20 }; $alias=$target.Alias; $probe=& $sshPath -o BatchMode=yes -o ConnectTimeout=15 $alias 'hostname; whoami; pwd' 2>$null; if($LASTEXITCODE -ne 0 -or $probe.Count -lt 2){ Write-Output 'TERMINAL_READY=NO'; Write-Output ('SSH_HOST_ALIAS=' + $alias); Write-Output 'ERROR=SSH alias connection failed'; exit 21 }; $remoteHost=$probe[0].Trim(); $remoteUser=$probe[1].Trim(); $deployTest=& $sshPath -o BatchMode=yes -o ConnectTimeout=15 $alias \"test -d /home/ubuntu/shopvivaliz-deploy/current && echo DEPLOY_OK || echo DEPLOY_NOT_FOUND\" 2>$null; $deployOk=($deployTest -contains 'DEPLOY_OK'); $meta=& $sshPath -o BatchMode=yes -o ConnectTimeout=15 $alias \"cd /home/ubuntu/shopvivaliz-deploy/current 2>/dev/null || exit 2; printf 'GIT='; git --version | head -n1; printf 'SHA='; git rev-parse HEAD; printf 'PHP='; php -r 'echo PHP_VERSION;' 2>/dev/null || echo unavailable\" 2>$null; $sha=''; $php=''; foreach($ln in $meta){ if($ln -like 'SHA=*'){$sha=$ln.Substring(4).Trim()}; if($ln -like 'PHP=*'){$php=$ln.Substring(4).Trim()} }; function HttpProbe([string]$u,[string]$label){ $fmt='%{http_code}|%{url_effective}|%{time_total}'; $r=& $sshPath -o BatchMode=yes -o ConnectTimeout=15 $alias \"curl -L -sS -o /dev/null --connect-timeout 10 --max-time 30 -w '$fmt' '$u'\" 2>$null; if($LASTEXITCODE -ne 0){ return @{Status='ERROR';Final='';Time=''} }; $p=($r | Select-Object -Last 1).Trim().Split('|'); return @{Status=$p[0];Final=$p[1];Time=$p[2]} }; $home=HttpProbe 'https://shopvivaliz.com.br/' 'HOME'; $cat=HttpProbe 'https://shopvivaliz.com.br/catalogo' 'CATALOGO'; $ant=HttpProbe 'https://shopvivaliz.com.br/catalogo?q=antique' 'ANTIQUE'; $dec=HttpProbe 'https://shopvivaliz.com.br/catalogo?q=decore' 'DECORE'; Write-Output ('TERMINAL_READY=' + $(if($deployOk -and $sha -and $php){'YES'}else{'NO'})); Write-Output ('SSH_HOST_ALIAS=' + $alias); Write-Output ('REMOTE_HOSTNAME=' + $remoteHost); Write-Output ('REMOTE_USER=' + $remoteUser); Write-Output 'DEPLOY_PATH=/home/ubuntu/shopvivaliz-deploy/current'; Write-Output ('DEPLOY_SHA=' + $sha); Write-Output ('PHP=' + $php); Write-Output ('HOME_HTTP=' + $home.Status); Write-Output ('CATALOGO_HTTP=' + $cat.Status); Write-Output ('ANTIQUE_HTTP=' + $ant.Status); Write-Output ('DECORE_HTTP=' + $dec.Status); Write-Output 'TUNNEL=READY'; Write-Output ('HOME_FINAL=' + $home.Final); Write-Output ('HOME_TIME=' + $home.Time); Write-Output ('CATALOGO_FINAL=' + $cat.Final); Write-Output ('CATALOGO_TIME=' + $cat.Time); Write-Output ('ANTIQUE_FINAL=' + $ant.Final); Write-Output ('ANTIQUE_TIME=' + $ant.Time); Write-Output ('DECORE_FINAL=' + $dec.Final); Write-Output ('DECORE_TIME=' + $dec.Time); Write-Output 'ERROR='",
  "requested_at": "2026-08-20T14:31:00Z",
  "reason": "Validate existing Windows SSH alias, production deploy path and VM-origin HTTP connectivity through the existing Fred-Win private relay without exposing secrets"
}
