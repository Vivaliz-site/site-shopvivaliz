{
  "action": "terminal",
  "shell": "powershell",
  "timeout": 120,
  "command": "$ErrorActionPreference='Stop'; Write-Output ('HOST=' + $env:COMPUTERNAME); Write-Output ('USER=' + $env:USERNAME); $log=Join-Path $env:TEMP 'desktop-commander-remote.log'; $existing=Get-CimInstance Win32_Process -Filter \"Name='node.exe'\" -ErrorAction SilentlyContinue | Where-Object { $_.CommandLine -match 'desktop-commander.*remote' }; if(-not $existing){ $args='-NoProfile -WindowStyle Minimized -Command \"npx --yes @wonderwhy-er/desktop-commander@latest remote *>> ''$log''\"'; Start-Process -FilePath 'powershell.exe' -ArgumentList $args -WindowStyle Minimized; Start-Sleep -Seconds 12 }; $running=Get-CimInstance Win32_Process -Filter \"Name='node.exe'\" -ErrorAction SilentlyContinue | Where-Object { $_.CommandLine -match 'desktop-commander.*remote' }; Write-Output ('REMOTE_AGENT_RUNNING=' + [bool]$running); if(Test-Path $log){ $tail=Get-Content $log -Tail 30 -ErrorAction SilentlyContinue; $needsAuth=[bool]($tail -match 'verify|pair|code|sign in|device'); Write-Output ('PAIRING_MAY_BE_REQUIRED=' + $needsAuth); $tail | ForEach-Object { Write-Output ('AGENT_LOG=' + $_) } } else { Write-Output 'PAIRING_MAY_BE_REQUIRED=UNKNOWN' }; exit 0",
  "requested_at": "2026-08-20T19:28:00Z",
  "reason": "Restore Fred-Win Desktop Commander tunnel and verify remote terminal availability"
}
