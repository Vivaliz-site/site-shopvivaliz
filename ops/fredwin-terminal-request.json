{
  "action": "terminal",
  "shell": "powershell",
  "timeout": 120,
  "command": "$ErrorActionPreference='Continue'; $s=(Get-Command ssh -ErrorAction Stop).Source; $svc=Get-Service ssh-agent -ErrorAction SilentlyContinue; if($svc -and $svc.Status -eq 'Stopped' -and $svc.StartType -ne 'Disabled'){try{Start-Service ssh-agent -ErrorAction Stop}catch{};$svc=Get-Service ssh-agent -ErrorAction SilentlyContinue}; $cfg=Join-Path $HOME '.ssh\\config'; $alias=''; $hostn=''; $user=''; if(Test-Path $cfg){$cur=''; foreach($r in Get-Content $cfg){$t=$r.Trim(); if(!$t -or $t.StartsWith('#')){continue}; if($t.StartsWith('Host ')){$cur=$t.Substring(5).Trim(); if($cur -match '[*? ]'){$cur=''}} elseif($cur -and $t.StartsWith('HostName ')){ $h=$t.Substring(9).Trim(); if($h -eq '137.131.156.17'){$alias=$cur;$hostn=$h} } elseif($alias -and $cur -eq $alias -and $t.StartsWith('User ')){$user=$t.Substring(5).Trim()} }}; if(!$alias -and (Test-Path $cfg)){foreach($r in Get-Content $cfg){$t=$r.Trim(); if($t.StartsWith('Host ')){$h=$t.Substring(5).Trim(); if($h -notmatch '[*? ]' -and $h -match '(?i)shopvivaliz|vivaliz|oracle|prod'){$alias=$h;break}}}}; Write-Output ('SSH_HOST_ALIAS=' + $alias); Write-Output ('SSH_CONFIG=' + $(if(Test-Path $cfg){'YES'}else{'NO'})); Write-Output ('SSH_AGENT=' + $(if($svc){$svc.Status}else{'NOT_INSTALLED'})); if($alias){$p=& $s -o BatchMode=yes -o ConnectTimeout=15 $alias 'hostname; whoami; pwd' 2>$null; if($LASTEXITCODE -eq 0){Write-Output ('REMOTE_HOSTNAME=' + $p[0]);Write-Output ('REMOTE_USER=' + $p[1]);Write-Output 'SSH_PROBE=OK'}else{Write-Output 'SSH_PROBE=ERROR'}}else{Write-Output 'SSH_PROBE=NO_ALIAS'}",
  "requested_at": "2026-08-20T14:35:00Z",
  "reason": "Identify and validate the existing ShopVivaliz SSH alias through the existing Fred-Win private relay"
}
