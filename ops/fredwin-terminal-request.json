{
  "action": "terminal",
  "shell": "powershell",
  "timeout": 120,
  "command": "$ErrorActionPreference='Stop'; $s=(Get-Command ssh -ErrorAction Stop).Source; $a='shopvivaliz-vm'; $cmd=\"cd /home/ubuntu/mei-mg-email && echo CHECK_BRT=$(TZ=America/Sao_Paulo date +%Y-%m-%dT%H:%M:%S%z); echo WORKER=$(sudo systemctl is-active mei-mg-email-worker.service 2>/dev/null || true); echo ENABLED=$(sudo systemctl is-enabled mei-mg-email-worker.service 2>/dev/null || true); echo TIMER=$(sudo systemctl is-active mei-mg-email-autorepair.timer 2>/dev/null || true); echo SENTINEL=$(sudo test -f /var/lib/mei-mg-email/sender_blocked.pause && echo present || echo absent); echo SENT15=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \\\"select count(*) from mei_email.envios where status::text in ('submitted','enviado') and enviado_em>=now()-interval '15 minutes';\\\"); echo SENT24=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \\\"select count(*) from mei_email.envios where status::text in ('submitted','enviado') and enviado_em>=now()-interval '24 hours';\\\"); echo PENDING=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \\\"select count(*) from mei_email.envios where status::text in ('pendente','pending','enviando','processing');\\\"); echo LATEST=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \\\"select coalesce(max(enviado_em)::text,'none') from mei_email.envios where status::text in ('submitted','enviado');\\\"); echo AS42004_LAST15M=$(sudo journalctl -u mei-mg-email-worker.service --since '-15 minutes' --no-pager | grep -Eic 'AS\\(42004\\)|bad outbound sender|5\\.1\\.8' || true)\"; $o=& $s -o BatchMode=yes -o ConnectTimeout=15 $a $cmd 2>$null; if($LASTEXITCODE -ne 0){throw 'SSH sender probe failed'}; $o | ForEach-Object {Write-Output $_}",
  "requested_at": "2026-08-20T14:59:00Z",
  "reason": "Read-only production email sender state via existing Fred-Win SSH alias; no secrets"
}
