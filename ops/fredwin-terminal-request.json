{
  "action": "terminal",
  "shell": "powershell",
  "timeout": 90,
  "command": "$ErrorActionPreference='Stop'; $ssh=(Get-Command ssh -ErrorAction Stop).Source; $alias='shopvivaliz-vm'; $remote=@'\ncd /home/ubuntu/mei-mg-email || exit 2\necho WORKER=$(sudo systemctl is-active mei-mg-email-worker.service 2>/dev/null || true)\nif sudo test -f /var/lib/mei-mg-email/sender_blocked.pause; then echo SENTINEL=present; else echo SENTINEL=absent; fi\necho SENT24=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \"select count(*) from mei_email.envios where status::text in ('submitted','enviado') and enviado_em>=now()-interval '24 hours';\")\necho PENDING=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \"select count(*) from mei_email.envios where status::text in ('pendente','pending','enviando','processing');\")\necho LATEST=$(sudo docker exec mei-mg-email-db psql -U postgres -d mei_mg_email -At -c \"select coalesce(max(enviado_em)::text,'none') from mei_email.envios where status::text in ('submitted','enviado');\")\n'@; $out=& $ssh -o BatchMode=yes -o ConnectTimeout=15 $alias $remote 2>$null; if($LASTEXITCODE -ne 0){throw 'remote state probe failed'}; $repo='C:\\site-shopvivaliz'; $report=Join-Path $repo 'reports\\fredwin-email-state-latest.txt'; New-Item -ItemType Directory -Force -Path (Split-Path $report) | Out-Null; $out | Set-Content -Encoding UTF8 $report; Push-Location $repo; try { git add -- 'reports/fredwin-email-state-latest.txt'; git commit -m 'ops: record Fred-Win minimal email state' 2>$null; git push origin HEAD:main 2>$null } finally { Pop-Location }; $out | ForEach-Object {Write-Output $_}",
  "requested_at": "2026-08-20T15:15:00Z",
  "reason": "Capture minimal sanitized sender state through the validated Fred-Win alias to diagnose why sending stopped"
}
