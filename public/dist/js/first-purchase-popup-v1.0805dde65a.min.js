(function () { 'use strict'; try { var pendingKey = 'shopvivaliz_pending_coupon'; var usedKey = 'shopvivaliz_first_coupon_used'; var pending = String(localStorage.getItem(pendingKey) || '').toUpperCase(); var used = String(localStorage.getItem(usedKey) || '').toUpperCase(); if (pending === 'PRIMEIRA10' || pending === 'PRIMEIRA15') { localStorage.removeItem(pendingKey); } if (used === 'PRIMEIRA10' || used === 'PRIMEIRA15') { localStorage.removeItem(usedKey); } } catch (error) {} })();