# Scripts de Manutenção

Contém auditoria, diagnóstico, segurança, rollback, health check e guardrails.

Arquivos equivalentes diretamente em `scripts/` são wrappers temporários. Toda rotina desta pasta deve ser registrada em `docs/knowledge/routines-registry.md`.
