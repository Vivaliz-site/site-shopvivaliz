=== 24-7-continuous-agent.yml ===
name: 24-7 Continuous Agent

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
    inputs:
      cycles:
        description: 'Quantidade de ciclos seguros do watchdog'
        required: false
        default: '1'


=== agent-continuous-task-processor.yml ===
name: PAUSADO - Agent Continuous Task Processor
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== ai-agent-review.yml ===
name: PAUSADO - Revisao dos Agentes IA
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== ai-autonomous-executor.yml ===
name: AI Autonomous Executor

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
    inputs:
      cycles:
        description: 'Quantidade de ciclos seguros do watchdog'
        required: false
        default: '2'


=== ai-pipeline-full.yml ===
name: PAUSADO - AI Pipeline Full
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== auditoria-vazamento-30min.yml ===
name: Auditoria de Vazamento (30 min)

on:
  workflow_dispatch:
    inputs:
      delay_minutes:
        description: "Aguardar quantos minutos antes de auditar"
        required: false
        default: "30"

permissions:
  contents: read

=== auto-approve-bot-prs.yml ===
name: Auto Approve Bot PRs

on:
  pull_request_target:
    branches:
      - main
    types:
      - opened
      - reopened
      - ready_for_review
      - synchronize


=== auto-commit.yml ===
name: Auto-Commit Changes

on:
  schedule:
    - cron: '*/30 * * * *'  # A cada 30 minutos
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

=== auto-ftp-deploy.yml ===
name: PAUSADO - Auto FTP Deploy
# Desabilitado 2026-07-03: quota GitHub Actions esgotada (deploy.yml ja cobre isso)
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== auto-git-pull.yml ===
name: PAUSADO - Auto Git Pull
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== auto-git-push.yml ===
name: PAUSADO - Auto Git Push
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== auto-task-generator.yml ===
name: PAUSADO - Auto Task Generator
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== auto-validation-and-fix.yml ===
name: PAUSADO - Auto Validation and Fix
# Desabilitado 2026-07-03: quota GitHub Actions esgotada (ci-autonomo-continuo.yml ja cobre isso)
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== automation-autonoma-24-7.yml ===
name: PAUSADO - Automacao Autonoma 24/7
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== autonomous-agents-24-7.yml ===
name: PAUSADO - Agentes Autonomos 24/7
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== autonomous-cycle.yml ===
name: PAUSADO - Ciclo Autonomo 24/7
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== autonomous-orchestrator.yml ===
name: PAUSADO - Autonomous Orchestrator
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== autonomous-proactive.yml ===
name: PAUSADO - Autonomous Proactive Agent
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== autonomous-task-execution.yml ===
name: PAUSADO - Autonomous Task Execution
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== autonomous-watchdog.yml ===
name: Autonomous Watchdog Monitor

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      cycles:
        description: 'Number of safe watchdog cycles to request'
        required: false
        default: '1'


=== ci-autonomo-continuo.yml ===
name: CI AUTONOMO CONTINUO

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */4 * * *"

permissions:
  contents: read
  checks: write

jobs:

=== copy-secrets-to-pipeline.yml ===
name: PAUSADO - Copy Secrets to Pipeline
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== deploy-olist-proxy.yml ===
name: PAUSADO - Deploy Olist Proxy
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== deploy-squad-chat.yml ===
name: PAUSADO - Deploy Squad Chat
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== deploy.yml ===
name: Deploy AutomÃ¡tico Seguro - Shop Vivaliz

# Desativado o gatilho automatico em push: a producao real e a VM Oracle
# (dev.shopvivaliz.com.br -> 137.131.156.17, sincronizada via cron
# git-auto-sync.py), nao o HostGator. Mantido workflow_dispatch para
# rodar manualmente se o HostGator voltar a ser usado no futuro.
on:
  workflow_dispatch:

permissions:
  contents: read


=== diag-tiny-api.yml ===
name: PAUSADO - Diagnostico Tiny API
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== ecommerce-multi-ai-build-24-7.yml ===
name: PAUSADO - E-commerce Multi-AI Builder 24/7
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== execute-update.yml ===
name: PAUSADO - ShopVivaliz Execute Update
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== export-olist-images-csv.yml ===
name: Exportar imagens Olist para CSV

on:
  workflow_dispatch:

jobs:
  export-olist-images-csv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4


=== external-smoke-test.yml ===
name: PAUSADO - ShopVivaliz External Smoke Test
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== fetch-shopee-listings.yml ===
name: Fetch Shopee Listings via Tiny API

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

permissions:
  contents: write
  pull-requests: write
  actions: write
  checks: write

=== force-deploy-now.yml ===
name: Force Deploy Now

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

=== git-auto-sync-validate.yml ===
name: PAUSADO - Git Auto Sync Validate
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== health-check.yml ===
name: PAUSADO - Health Check
# Desabilitado 2026-07-03: quota GitHub Actions esgotada (ci-autonomo-continuo.yml ja cobre isso)
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== hourly-status-email.yml ===
name: PAUSADO - Hourly Status Email
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== medusa-eha-next-step-30min.yml ===
name: PAUSADO - Medusa EHA Next Step 30min
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== monitor-actions-and-prs.yml ===
name: Monitor & Fix Actions & PRs

on:
  schedule:
    - cron: '*/30 * * * *'  # A cada 30 minutos
  workflow_dispatch:

jobs:
  monitor-and-fix:
    runs-on: ubuntu-latest
    permissions:
      actions: read

=== monitor-chat-responder.yml ===
name: PAUSADO - Monitor Chat Responder
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== monitor-chat-responses.yml ===
name: PAUSADO - Monitor Chat Responses
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== olist-sync.yml ===
name: PAUSADO - Olist Sync
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== optimize-shopee-listings.yml ===
name: Optimize Shopee Listings via Tiny API

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'   # diariamente Ã s 03h UTC

permissions:
  contents: write
  pull-requests: write
  actions: write
  checks: write

=== package-v9-2-84.yml ===
name: PAUSADO - Package v9-2-84
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== parallel-trio-executor.yml ===
name: Parallel Trio Executor

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      cycles:
        description: 'Quantidade de ciclos seguros do watchdog'
        required: false
        default: '3'


=== secret-scan.yml ===
name: PAUSADO - Secret Scan
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== setup-branch-protection.yml ===
name: PAUSADO - Setup Branch Protection
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== setup-secrets.yml ===
name: PAUSADO - Setup Secrets
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== shopee-email-pipeline.yml ===
name: Shopee Email Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  shopeedelivery:
    runs-on: ubuntu-latest
    timeout-minutes: 90

=== shopee-image-upload.yml ===
name: Shopee - Upload Imagens para Media Space

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (nÃ£o faz upload real na Shopee)"
        required: false
        default: "true"
      max_products:
        description: "MÃ¡ximo de produtos (0 = todos)"
        required: false

=== shopee-media-space-repair.yml ===
name: Shopee Media Space Repair

on:
  workflow_dispatch:
    inputs:
      result_xlsx:
        description: "Caminho no repo da planilha Result_*.xlsx"
        required: true
        default: "imports/shopee-media-space/Result_shopee_mass_update_media_info_604371761_20260702124508.xlsx"
      mapping_xlsx:
        description: "Caminho no repo da planilha mapeamento_shopee_geradas.xlsx"
        required: true

=== shopee-upload-com-secrets.yml ===
name: Shopee - Upload Imagens (Com Secrets)

on:
  schedule:
    # Executar a cada 6 horas
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  shopee-upload:
    runs-on: ubuntu-latest


=== shopvivaliz-ai-pipeline.yml ===
name: PAUSADO - ShopVivaliz AI Pipeline
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== shopvivaliz-qa.yml ===
name: ShopVivaliz QA

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: write

=== site-monitor-autofix.yml ===
name: PAUSADO - Site Monitor Autofix
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== surgical-file-sync.yml ===
name: Sincronizacao Cirurgica de Arquivos (uso unico)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  recheck:
    name: Reconferir runtime-secrets.php
    runs-on: ubuntu-latest

=== sync-olist-6h.yml ===
name: Sincronizar Olist (a cada 6 horas)

on:
  schedule:
    # A cada 6 horas: 00:00, 06:00, 12:00, 18:00 UTC
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:

jobs:
  sync-olist:
    runs-on: ubuntu-latest


=== sync-olist-images-site.yml ===
name: Sincronizar imagens Olist no site via Tiny V2 ao vivo

on:
  workflow_dispatch:
    inputs:
      mapping_csv_gzip_base64:
        description: CSV compactado com gzip+base64 contendo ao menos ID e SKU dos produtos para consulta ao vivo na Tiny V2
        required: true
        type: string
      dry_run:
        description: Executar sem vincular banco no servidor
        required: false

=== sync-olist-images-v2.yml ===
name: Sincronizar Imagens Olist v2 (WebScraping)

on:
  schedule:
    # Executa a cada 6 horas
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      csv_file:
        description: 'CSV de entrada (sku,olist_product_id,nome_produto)'
        required: false
        default: 'produtos_ids_exemplo.csv'

=== sync-oracle-vm-secrets.yml ===
name: Sincronizar secrets na VM Oracle (Ubuntu)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync-secrets:
    name: Escrever .env de producao via SSH
    runs-on: ubuntu-latest

=== sync-prices-tiny.yml ===
name: PAUSADO - Sync Prices Tiny
# Desabilitado 2026-07-03: quota GitHub Actions esgotada
on:
  workflow_dispatch:
jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow pausado para economizar quota Actions."

=== sync-shopee-6h.yml ===
name: Sincronizar Shopee (a cada 6 horas)

on:
  schedule:
    # A cada 6 horas: 00:00, 06:00, 12:00, 18:00 UTC
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:

jobs:
  sync-shopee:
    runs-on: ubuntu-latest


=== sync-stock-tiny.yml ===
name: Sincronizar estoque real da Tiny

# O webhook em api/tiny/stock-webhook.php ja atualiza o estoque em
# tempo real a cada mudanca na Tiny. Este workflow roda 1x/dia so
# como rede de seguranca (webhook perdido, produto novo sem SKU
# mapeado ainda, etc.), nao como mecanismo primario.
on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

permissions:

=== v12-execucao-automatica.yml ===
name: V12 ExecuÃ§Ã£o AutomÃ¡tica

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  run_pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: read

